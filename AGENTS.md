## 本プロジェクトの概要と目的

このプロジェクトは、中将棋 (chu shogi) という日本のボードゲームをRustで実装することを目指している。目的は以下のとおりである。
- 中将棋の合法手生成機能を提供するライブラリとして使える。
- 外部（GUI・CUI・プログラム）から呼び出して使える中将棋対局プログラムとして使える。その実現のために、ライブラリを内部的に利用しつつ探索部および評価関数も実装する。

中将棋には様々なルールがあり、文献や実装によって採用しているルールには差異がある。このプロジェクトでは、日本中将棋連盟のルールに準拠することを前提とする。ただし、その他のルール流儀もサポートし、何らかのオプションによって切り替える機能をつける予定である。このプログラムでサポートする競技規則およびローカルルールのリストは @RULES.md である。

このエンジンを呼び出すユースケースとして、以下の事例を想定している。
- Xboard/CECPプロトコルを介してHachu engineと接続したい。
- USIプロトコルを介してlishogi botとして対戦できるようにしたい。
- WinBoardやlishogiが対応していないローカルルールにも対応できるよう、開発用の対局管理マネージャの自作も想定する。

開発用のGUIアプリは別gitリポジトリ `minase-gui` として開発中であり、カレントディレクトリに `minase-gui` へのシンボリックリンクを作成している。

まずはライブラリとして使えることを優先し、探索部および評価関数は合法手生成器が完全になってから実装するつもりでいる。

## 開発ドキュメントの運用

マイルストーンの設計書は docs/plans/ に置き、進捗の正は docs/ROADMAP.md のマイルストーン状態表で管理する。新しいマイルストーンに着手するときは docs/plans/ に設計書を作成して状態表へ行を追加し、完了時に状態表と現在地の節を更新する。完了したマイルストーンの詳細な記録はROADMAP.mdに書かず、各設計書に残す。

## このプロジェクトのソースコード運用

- `src/core/`には、RULES.mdだけから正しさを検証できるコードのみを置く。表記・プロトコル・探索・評価はcoreの外に置く（経緯と配置方針は docs/plans/src-layout.md）。

## 作業全般の共通ポリシー

- 実装や調査などのタスクを実行する際、subagentsを積極的に使ってください。特に、あなたがClaudeの場合、プログラムの具体的な実装に関わるタスクでは積極的にcodex subagentに作業を移譲してください。

## 棋力測定のポリシー

- 探索・評価・時間管理など棋力に影響し得る変更の採否は、SPRTによる自己対局測定で判定する。測定はコミット対コミットの対局で行い、ハーネスに機能比較のスイッチを追加してはならない。測定手順・標準コマンド・統計的契約・記録項目は @docs/sprt.md を参照。

## コーディング時のポリシー

- 早すぎる最適化、測定なしの最適化は禁止する。ボトルネック議論では実測データか再現手順をセットで提示すること。
- KISS（簡潔かつ単純にする）原則に従う。同じことをもっと簡潔なやり方・最小限のコード量で実装できないか考える。
- YAGNI（必要になるまで書かない）原則に従う。将来用のフィールド/フラグ追加、未使用コードの温存は禁止。

## コメント規約

src/ 全体（テストコードを除く）に次の規約を適用する（2026-08-13確定）。

- 形式は日本語のrustdoc（`///`と`//!`）とする。型・全フィールド・全バリアント・固有メソッド（pub/private問わず）とファイル冒頭の`//!`を網羅する。`fmt`・`From`・`Iterator`・演算子・`Default`などの定型trait実装には付けない。
- コメントの主張は検証可能な文書へ接地させる。coreはRULES.mdの定義語を主、条番号を従として併記する（例:「付け喰い(第16条)」、項レベルまでは原則書かない）。core外は領域ごとの規範文書を参照する。notation・protocolはUSI/CECPの表記仕様とdocs/配下の対応文書、searchは学会定訳とdocs/plans/search.md、stats・match_runnerはdocs/sprt.mdとfishtestを、文書名＋概念名の粒度で引く。
- 行内コメント（`//`）は「why＋区切り」に限る。規則上の根拠、不変条件・前提、外部互換性の都合、長い関数の段落区切りだけを書き、処理の言い換えは書かない。必要駆動で付けるため、ファイル間の密度の偏りは許容する。
- rustdocの節見出し（`# Panics`、`# Errors`、`# Examples`）は英語標準形を維持する。本文は日本語で書く。
- `#![warn(missing_docs)]`を採用している。この lint は公開項目しか検査しないため、private項目への doc はレビューで守る。
- この種のコメント執筆は、用語・文体の一貫性を保つためcodex subagentへ委譲せずClaudeが直接書く。


## 本プロジェクトの概要と目的

このプロジェクトは、中将棋 (chu shogi) という日本のボードゲームをRustで実装することを目指している。目的は以下のとおりである。
- 中将棋の合法手生成機能を提供するライブラリとして使える。
- 外部（GUI・CUI・プログラム）から呼び出して使える中将棋対局プログラムとして使える。その実現のために、ライブラリを内部的に利用しつつ探索部および評価関数も実装する。

中将棋には様々なルールがあり、文献や実装によって採用しているルールには差異がある。このプロジェクトでは、日本中将棋連盟のルールに準拠することを前提とする。ただし、その他のルール流儀もサポートし、何らかのオプションによって切り替える機能をつける。このプログラムでサポートする競技規則およびローカルルールのリストは @RULES.md である。

開発用のGUIアプリは別gitリポジトリ `minase-gui` として開発中であり、カレントディレクトリに `minase-gui` へのシンボリックリンクを作成している。
lishogiのBotアカウントとしてminaseを動かす配備一式（Dockerイメージ、Lishogi-Botの設定、運用手順）は別gitリポジトリ `minase-lishogi-bot` に置き、カレントディレクトリに同名のシンボリックリンクを作成している。minase本体はUSIエンジンとして呼ばれる側であり、lishogi接続の知識を持たない。

## 作業全般の共通ポリシー

- 互いに独立した調査や実装、または大量の読み込みを要する作業は、subagentsへ任せる。
- あなたがClaudeの場合、プログラムの具体的な実装はcodex subagentへ委任し、Claude自身は設計の確定、レビュー、コミット、およびRULES.mdとdocs/の編集を担当する。計画書を作成したら、codex subagentにレビューを求める。
- 新しく設計書の内容を実装する際は、設計書1枚につき1ブランチの単位で新規にmasterからブランチを切り、そこで作業を行なってください。worktreeを使用することも可能です。
- 開発ドキュメント、コミットメッセージ、リリース、および棋力測定は @CONTRIBUTING.md の規約に従う。測定手順の詳細は @docs/guides/sprt.md にある。

## コーディング時のポリシー

- 早すぎる最適化、測定なしの最適化は禁止する。ボトルネック議論では実測データか再現手順をセットで提示すること。
- KISS（簡潔かつ単純にする）原則に従う。同じことをもっと簡潔なやり方・最小限のコード量で実装できないか考える。
- YAGNI（必要になるまで書かない）原則に従う。将来用のフィールド/フラグ追加、未使用コードの温存は禁止。


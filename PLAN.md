# 中将棋の合法手生成の設計書（第2版）

## 1. 目的と適用範囲

本書は、中将棋エンジン minase のマイルストーン「合法手生成、make move、unmake move の成立」の設計を定める。
規則の正は RULES.md（中将棋統合競技規則案 第5版）であり、本書と RULES.md が矛盾する場合は RULES.md を優先する。
探索部、評価関数、時間管理、棋譜入出力は対象外とする。

第1版の設計書は RULES.md 制定前に書かれ、現行の src/ はそれに基づいている。
第2版は、第1版のうち盤面表現と利き計算の基盤を引き継ぎ、指し手表現と規則判定を全面的に再設計する。

引き継ぐ基盤は次のとおりである。

- `Square`、`Bitboard`（3×64ビット、段あたり16ビット）、`Direction` と方向シフトは変更しない。
- `Color`、`PieceKind`、`PieceCode`、および `Position` の冗長表現（マス配列と駒種別ビットボード）は変更しない。
- `attacks/` のレイ表、固定移動表、移動プロファイルは変更しない。

これらの仕様は実装とテストを正とし、本書では再述しない。

## 2. 設計判断の一覧

本版は次の8点を決定として採用する。
各判断の根拠と詳細は括弧内の節で述べる。

| 判断 | 決定 |
|---|---|
| 合法手の定義 | 王駒の安全を合法性条件に含めない（3節） |
| 指し手表現 | 経路構造の enum とし、捕獲は盤面から導出する（4節） |
| 状態スコープ | 盤面、手番、先獅子トリガーまでを実装する（5節） |
| 先獅子の計算 | トリガーは make 時に記録し、足判定は生成時に遅延する（6節） |
| ローカルルール機構 | 設定構造体とし、今回は標準規則のみ実装する（7節） |
| 検証 | 条文対応テスト、参照実装、ランダムプロパティ、perft 照合で完了条件を定義する（9節） |
| 規則解釈の境界 | 採用解釈を明文化して実装を先行し、文献調査を並行する（10節） |
| 進め方 | 本書を仕様として実装を段階発注する（11節） |

## 3. 合法手の定義

**合法手**は、RULES.md 第2条12項に従い、駒の移動能力、捕獲規則、成り規則、獅子の捕獲制限（第12〜15条）、および採用ローカルルールをすべて満たす着手と定義する。
王駒の安全は合法性条件に含めない。
第8条9項が王手放置と自殺手を合法と定めているためである。

この定義により、旧設計の「疑似合法手を1手ずつ適用して王駒の安全を検査する」ループは不要になる。
現行 `rules.rs` の `position_is_legal_after_move` と `movegen/legal.rs` の全手 make/unmake 検査は削除する。
生成器は規則検査を生成中に完結させ、`generate_moves` の一本で合法手を直接返す。

詰み、王駒の捕獲による終局（第17条）、合法手がない場合の敗北（第19条）は終局判定であり、本マイルストーンの対象外とする。

## 4. 指し手表現

指し手は経路構造の enum で表す。

```rust
#[derive(Clone, Copy, PartialEq, Eq, Hash, Debug)]
pub enum Move {
    /// 1段階移動。走り、固定移動、麒麟や鳳凰の固定跳び、
    /// 獅子や角鷹の1歩停止を含む。
    Step   { from: Square, to: Square, promote: bool },
    /// 2段階移動。獅子、角鷹の前方、飛鷲の前斜めが行う。
    /// to == from が帰還であり、mid に敵駒があれば居喰い、
    /// mid が空升なら「じっと」になる。
    Double { from: Square, mid: Square, to: Square, promote: bool },
    /// 獅子力の直接跳び。中間升を検査せず2升目へ進む。
    Jump   { from: Square, to: Square, promote: bool },
}
```

捕獲升はフィールドに持たない。
適用時に、`mid` と `to` の上にある敵駒を取ると定める（`to == from` の場合は `mid` のみ）。
これにより、同じ物理的行動に対する表現が1つになり、旧実装が居喰いを `Move::igui` と `lion_double(from, x, from, ..)` の2通りで重複生成していた問題が型ごと消える。
`MoveKind`、`SquareOrNone`、`capture1`、`capture2` は廃止する。

各バリアントの使い分けは次のとおりである。

- **Step**：単一の移動先だけで定まる着手すべてに使う。麒麟の縦横2升跳びのように、2段階の代替経路を持たない固定跳びも Step で表す。
- **Double**：第1段階と第2段階を持つ着手に使う。第11条と第10条の帰還、居喰い、じっと、2回捕獲をこの形で表す。
- **Jump**：獅子（第11条5〜7項）、角鷹、飛鷲（第10条1項d）の直接跳びに使う。同じ最終升へ到達する Double とは中間捕獲の有無が異なるため、別の着手として区別する（第4条6号、第28条8号）。

型は経路の幾何（隣接性や方向の整合）を強制しない。
幾何の正しさは生成器が保証し、`make_move` は debug ビルドで検査する。
指し手のサイズ制約（旧設計の8バイト固定）は設けず、pack は探索実装時に計測へ基づいて再検討する。

成りフラグは着手全体の終了後に適用する（第16条9項）。
第1段階で成らないこと（第16条10項）は、移動能力を現在の駒種で計算する生成器の構造が保証する。

着手データとしての「捕獲される駒」（第4条）は、適用前局面と `Move` の組から一意に導出できる。
棋譜出力（第28条）もこの導出で行い、居喰いとじっとの区別は `mid` 上の駒の有無から得る。

## 5. ゲーム状態と make/unmake

`Position` は次の状態を保持する。

```rust
pub struct Position {
    board: [PieceCode; RAW_SQUARE_COUNT],
    occupied: Bitboard,
    by_color: [Bitboard; COLOR_COUNT],
    by_kind: [[Bitboard; PIECE_KIND_COUNT]; COLOR_COUNT],
    side_to_move: Color,
    /// 直前手で獅子以外の駒が獅子を取った升。先獅子（第14条）の
    /// トリガーであり、該当しない手の後は None に戻る。
    lion_taken_by_non_lion: Option<Square>,
}
```

現行の `last_move: Option<MoveHistory>` は廃止する。
履歴全体を持つと、合法手集合に影響しない情報が同一局面の比較（第20条）へ混入するためである。
`lion_taken_by_non_lion` は合法手集合に影響する唯一の一時状態である。
ただし `PartialEq` の全フィールド比較は「状態の完全一致」であり、第20条の同一局面判定そのものではない。
標準規則の先獅子判定は Some か None かの区別しか使わないため、トリガーの捕獲升だけが異なる2局面は、合法手集合が同一でも `PartialEq` では区別される。
反復判定（次マイルストーン）と局面ハッシュ（第27条）では、採用ルールに応じてトリガーを射影した局面キー（標準規則では Some か None の1ビット、L1 と L2 では升の値）を使う。
反復履歴、対局結果、駒枯れ判定待ち、P1 と P2 の成る権利（第3条の残り）は次マイルストーンで `Position` の外側の対局管理層に置く。

make/unmake は次の形にする。

```rust
pub struct Undo {
    mv: Move,
    moved_piece_before: PieceCode,
    captured: [Option<CapturedPiece>; 2],
    previous_lion_taken: Option<Square>,
}

impl Position {
    pub(crate) fn make_move_unchecked(&mut self, mv: Move) -> Undo;
    pub(crate) fn unmake_move(&mut self, undo: Undo);
    pub fn try_make_move(&mut self, mv: Move, ...) -> Result<Undo, IllegalMove>;
}
```

`Undo` は適用した `Move` を内包し、`unmake_move` は `Undo` だけを受け取る。
make が失った差分情報と、その差分を生んだ指し手が1つの値として運ばれるため、異なる手との誤った組合せは構成できない。
`Undo` は値で消費されるので、同じ `Undo` による二重の取り消しはコンパイルエラーになる。
`Move` の埋め込みによる `Undo` の増加は数バイトのスタックコピーであり、性能への影響はない。

適用手順は第5条の順序に従う。
第1段階の捕獲を反映し、第2段階を適用し、最後に成りを適用して手番を反転する。
`lion_taken_by_non_lion` の更新は駒種の比較だけで決まる機械的事実であり、利き計算を必要としない。
したがって make/unmake は規則層に依存しない。

`make_move_unchecked` と `unmake_move` の往復後、局面は全フィールドで元と一致しなければならない。
不正な着手が `try_make_move` に入力された場合、局面を変更しない（第23条1項）。

### 5.1 増分更新の原則

盤面更新は増分更新を原則とする。
探索は1つの `Position` に make と unmake を数百万回適用し続ける前提であり、盤面更新の効率はエンジン全体の性能を直接決めるためである。

- `board`、`occupied`、`by_color`、`by_kind` は、着手で影響を受ける升だけを `put_piece` と `remove_piece` 経由で更新する。全升の走査や再集計は行わない。
- `unmake_move` は `Undo` に保存した差分から復元する。局面のクローンや情報の再計算はしない。
- 局面のコピーは、対局管理や検証など低頻度の場面に限る。テストの全フィールド比較（9節）は正しさの検査であり、この原則の例外ではない。
- debug ビルドの make と unmake の末尾では、不変条件検査（`validate`）を `debug_assert!` で実行する。全升走査を含むが、release ビルドでは除去されるため、検証用の例外として認める。
- 将来の派生情報にも同じ原則を適用する。局面ハッシュ（第27条）は XOR による増分更新とし、旧値を `Undo` に積む。評価関数の差分計算を導入する場合も同じ形で拡張する。

規則判定が使う仮想盤面にも、この原則を適用する。
足判定（6.2節）で必要になる捕獲後の盤面は、`Position` 全体のクローンではなく、ビットボードの局所コピー（現行 `LocalOccupancy` と同じ方式）で構成する。
現行 `lion_capture_is_legal` が判定のたびに `Position` をクローンしている箇所は、この方式へ置き換える。

## 6. 獅子規則の判定

### 6.1 先獅子のトリガーと足判定の分担

先獅子（第14条）の判定は、記録と評価に分ける。

**記録**は `make_move` が行う。
獅子以外の駒（麒麟が同じ着手で獅子に成る場合を含む。第14条6項）が相手の獅子を取ったとき、その捕獲升を `lion_taken_by_non_lion` に記録する。
獅子が獅子を取った場合は記録しない（第14条5項）。
該当しない着手の後は None に戻す（第14条4項）。
標準規則の判定には Some か None かの区別だけで足りるが、将来の L1 と L2 が捕獲升を必要とするため升も保存する。

**評価**は合法手生成時に行う。
`lion_taken_by_non_lion` が Some のとき、手番側は「足のある相手獅子」を取る手を生成しない。
足がない獅子は取れる（第14条2項）。
付け喰いに該当する手は禁止の対象外とする（第15条7項）。

### 6.2 足の判定

**足**（第12条）は、対象の獅子が取られた直後に、取った駒を取り返せる味方の利きと定義される。
実装は、当該の捕獲手を適用した後の仮想盤面で、取られた側が捕獲駒の升に利きを持つかで判定する（第12条4項）。
獅子が盤上から除かれた後の盤面を使うため、裏足（第2条7項）は走り駒の利きとして自然に通る。

第12条3項の「取り返す駒が合法的に移動できること」については、次の解釈を採用する。
取り返しは疑似的な捕獲手の存在で判定し、その捕獲手自体への獅子捕獲制限の再帰は適用しない（深さ1で打ち切る）。
この解釈は10節の調査対象であり、調査結果と食い違えば条文テストごと更新する。

### 6.3 獅子による獅子の捕獲

獅子で相手の獅子を取る手（第13条）は、着手前の両獅子のチェビシェフ距離で分岐する。

- 距離1（隣接）の捕獲は、足の有無にかかわらず合法とする（第13条1項）。
- 距離2の捕獲は、取られる獅子に足がなければ合法とし、足があれば原則として不可とする（第13条2〜3項）。
- 足がある場合でも、付け喰いに該当すれば合法とする（第15条4項）。

**付け喰い**（第15条）は、`Double` の第1段階で mid 上の価値ある駒（歩兵と仲人以外）を取り、第2段階で相手の獅子を取る手と解釈する。
これに加えて、動かす駒が獅子であること（第15条2項）、および着手前の自獅子と取られる獅子のチェビシェフ距離が2であること（第15条1項の「1升を隔てた獅子同士」）を構成要件とする。
隣接する相手獅子を別の升経由の2段階で取る手は付け喰いではない。
新しい指し手表現では第1捕獲が構造的に mid 上の駒に限られるため、「両獅子の間にある駒」の位置条件は経路上の捕獲として表現される。
この解釈の妥当性も10節の調査対象とする。
付け喰いの例外は着手全体に適用する。
1手で相手の獅子を2枚取る付け喰いでは、中間で取る獅子に足があっても、着手全体を先獅子の禁止対象外とする（第14条7項、第15条7項）。

第15条8〜9項には特に注意する。
相手獅子の唯一の足が歩兵または仲人である場合、第1段階でその駒を取っても足は消滅したと扱わず、第2段階の獅子捕獲は不可とする。
実装は、捕獲後の仮想盤面での利き判定に、「第1段階で取られた歩兵または仲人が、取られる前の升から最終到達升へ利いていたか」の論理和を加える形で行う。
取られた駒の占有ビットを仮想盤面へ戻す方式は採らない。
占有まで復元すると、その駒の除去によって開通するはずの走り駒の足（第12条4項）を遮蔽し、第13条2項が禁じる距離2の捕獲を過剰に許すためである。

### 6.4 修正対象となる現行実装の不一致

書き換えの起点として、確認済みの不一致を列挙する。

| 条番号 | 現行の挙動 | 該当箇所 |
|---|---|---|
| 第7条 | 初期配置で獅子と奔王の筋が逆になっている | `position.rs` の `initial` |
| 第2条12項、第8条9項 | 王駒が次に取られる手を非合法として除外している | `rules.rs` の `position_is_legal_after_move` |
| 第10条1項d | 角鷹と飛鷲の直接跳びを、中間升が空升または敵駒のときに生成していない | `movegen/lion.rs` の `generate_lion_like` |
| 第11条、第28条 | 居喰いを2通りのエンコードで重複生成している | `movegen/lion.rs` の `generate_lion` と `generate_lion_like` |
| 第12条3〜4項 | 先獅子の足判定が捕獲後の仮想盤面を使わず、現在盤面の利きだけを見ている | `rules.rs` の `previous_move_protects_lion_from_recapture` |
| 第14条7項、第15条7項 | 先獅子の拒否を付け喰いの認定より先に適用し、正当な付け喰いまで拒否している | `rules.rs` の `special_move_is_legal` |
| 第15条8〜9項 | 唯一の足である歩兵を取ると獅子捕獲が通る（L3の挙動） | `rules.rs` の `lion_capture_is_legal` |
| 第15条1項 | 第1捕獲の位置を検査せず付け喰いを認定している | `rules.rs` の `is_tsukegui` |
| 第16条1項 | 経由升だけが敵陣に入る着手にも成りを認めている（文献と Lishogi は始点と終点で判定する） | `rules.rs` の `promotion_choice` |
| 第20条 | 直前手の履歴全体が局面の等価性に混入している | `position.rs` の `last_move` |

## 7. ローカルルール機構

現行の `dyn RuleSet` トレイトは廃止し、採用コードを保持する設定構造体に置き換える。

```rust
pub struct Rules {
    lion: LionRule,            // L0（標準）、L1、L2、L3
    promotion: PromotionRule,  // 標準、P1、P2
    lance_rescue: bool,        // P3
    go_between_rescue: bool,   // P4
    repetition: RepetitionRule,// 標準、R1、R2
    capture_ending: bool,      // E1
    no_bare_rule: bool,        // E2
}
```

`Rules` の構築は対局開始時に一度だけ行い（第1条5項）、矛盾する組合せ（L0とL1、P1とP2、R1とR2）を拒否する（第25条7項）。
本マイルストーンで実装するのは標準規則（L0相当）のみとし、未実装コードの指定は構築時に明示的なエラーで拒否する。
黙って標準規則へフォールバックすることはしない。

生成器と規則関数は `&Rules` を受け取る。
ルール差分ごとにトレイト実装を増やす形は、コードの列記（第25条）や矛盾検査と噛み合わないため採らない。

## 8. 着手生成の構成

モジュール構成は次のように変わる。

- `movegen/normal.rs` は1段階移動を `Move::Step` として生成する。役割は現行と同じで、指し手の構築部分だけが変わる。
- `movegen/lion.rs` は獅子、角鷹、飛鷲の `Double` と `Jump` を生成する。居喰いの重複生成を排除し、局所占有（`LocalOccupancy`）による第2段階の生成は維持する。
- `movegen/legal.rs` の全手検査ループは削除する。`IllegalMove` と `try_make_move` は残し、生成済み合法手との照合で検証する。
- 獅子規則の検査（6節）は生成中に適用する。足判定に使う `square_is_controlled` は規則層に残す。

生成の手順は次のとおりである。

1. 手番側の各駒について、移動プロファイルから1段階移動を生成する。
2. 獅子力を持つ駒（獅子、角鷹、飛鷲）について、2段階移動と直接跳びを生成する。直接跳びは中間升の占有にかかわらず常に候補とする（第10条1項d、第11条5〜7項）。
3. 獅子を取る手に第13〜15条の検査を適用し、先獅子の禁止（第14条）を適用する。
4. 各着手に成りの選択肢（第16条）を展開する。

成りの判定は現行の `promotion_choice` の構造を引き継ぐ。
敵陣への進入、敵陣内または敵陣から出る際の捕獲、歩兵の最奥段到達で成りを選択できる（第16条1項、3項、5項）。
敵陣への進入は着手の始点と終点で判定し、2段階移動の経由升だけが敵陣に入る場合は成れない（10節）。
成れない着手への成り指定は `try_make_move` が拒否する（第16条13項）。

## 9. 検証

検証は4本柱で行い、その合格を本マイルストーンの完了条件とする。

**条文対応テスト**：RULES.md の条項ごとに境界局面のテストを対応させる。
対象は第7〜16条（初期配置、着手、成り）、第22〜23条（不正入力の拒否）である。
各テストには対応する条番号を明記し、6.4節の不一致それぞれに回帰テストを置く。
採用解釈（6.2節と6.3節）もテストとして固定する。

**参照実装クロスチェック**：ビット演算を使わない座標走査の実装を、利きの比較から着手列挙の比較まで拡張する。
ランダム局面で全駒種の生成結果を突き合わせ、ビットボード層の境界バグを検出する。

**ランダムプロパティテスト**：決定的な軽量乱数（xorshift を手書きし、依存クレートは追加しない）でランダム局面と手順を作り、次を検査する。

1. すべての生成手について make/unmake の往復が局面を全フィールドで復元する。
2. 適用後の局面が `validate` の不変条件を満たす。
3. 生成手に重複がない（同一の `Move` 値が2度現れない）。
4. 長い手順を適用して逆順に取り消すと初期局面へ完全に戻る。

**perft 照合**：局面数え上げ検査（perft）を実装し、Lishogi の実装（shogiops）のテストにある参照値と照合する（10節）。
対象は、初期局面の深さ1（36手）と深さ2（1296局面）、盤上1枚の獅子（88手）、麒麟成の獅子（88手）、角鷹（41手）、飛鷲（40手）の各局面である。
数え方は移動元、経由升、最終升の組ごとに1手とし、本設計の `Move` 値の個数と一致させる。
照合に合格した時点で、より深い深さを含む自己 perft 値を回帰値として固定する。

## 10. 規則解釈の境界と調査結果

規則解釈の境界は、採用解釈を条文テストに固定した上で実装を先行し、文献調査で裏を取る方針とした。
2026年7月29日に連盟の総則編と補足編、岡崎「中将棋の指し方」、英語文献、および Lishogi の実装（shogiops）を対象とする調査を実施し、次の結論を得た。

確定した解釈は次の3点である。

1. 第12条3項の「取り返す駒が合法的に移動できること」は、疑似的な捕獲手の存在で判定し、獅子捕獲制限の再帰は適用しない（深さ1打ち切り。6.2節）。英語版 Wikipedia が再帰非適用を明文で述べ、shogiops も利きベースの判定で一致する。連盟資料はこの境界に記述がない。
2. 第15条1項の「両獅子の間にある駒」は、2段階移動の第1段階で取る mid 上の駒と解釈する（6.3節）。英語文献は「第1段階での捕獲」として定義し、shogiops も第1捕獲の駒種だけを検査する。連盟資料は「間」の幾何を規定していないため、直線限定の解釈が現れた場合に備えて条文テストへ解釈を明記する。
3. 敵陣への進入の成り（第16条1項）は、着手の始点と終点で判定する。経由升だけが敵陣に入る2段階移動では成れない。当初の採用解釈（経由升の進入で成り可）はどの資料にも支持がなく、Muller の成りルール比較と shogiops の実装がいずれも始点と終点だけを参照するため、解釈を反転した。経由升を含めている現行実装は修正対象である（6.4節）。なお標準規則の駒立てでは、2段階移動を行える駒（獅子、角鷹、飛鷲）はいずれも成れないため、この境界が実戦の合法手に現れることはない。

perft の参照値は shogiops のテストに存在することが判明した。
初期局面の深さ1が36手、深さ2が1296局面、盤上に獅子1枚だけの局面が88手であり、数え方は移動元、経由升、最終升の組ごとに1手という本設計の Move 表現と同型である。
この照合は9節の検証へ採用した。

未解決のまま残る論点は次のとおりである。

1. 1手で獅子を2枚取る着手（角鷹または飛鷲の2回捕獲）における先獅子トリガーの表現。標準規則では Some か None の区別で足りるが、L1 と L2 の実装時に捕獲升の一意性が問題になる。
2. 先獅子の禁止対象が「取った側に残る獅子」複数枚に及ぶ場合の扱い。トリガーと盤面から遅延評価するため表現上の制約はないが、規則上の意図は未確認である。
3. 先獅子の禁止下で、制限側の獅子が隣接する相手獅子を取り返す着手の扱い。第14条1項の文理どおり禁止として実装し、テストで固定するが、獅子の直接交換を例外とする歴史的な読みがあるため文献調査の対象とする。
4. 居喰いとじっとの棋譜表記（第28条）。棋譜実装のマイルストーンで扱う。

## 11. 実装順序と発注単位

実装は次の4段階に分け、各段階で `cargo test` の全緑を受入条件とする。
すべての段階で5.1節の増分更新の原則を受入条件に含める（派生情報の全再計算と、高頻度経路での `Position` のクローンを認めない）。
発注時は本書と RULES.md を仕様として渡す。

**第1段階（指し手と局面）**：`Move` enum と `Undo` を導入し、`make_move_unchecked` と `unmake_move` を書き換える。
`MoveKind`、`SquareOrNone`、`MoveHistory` を削除し、`lion_taken_by_non_lion` を導入する。
初期配置の獅子と奔王の入れ替わりもこの段階で修正する。
既存テストを新表現へ追随させる。

**第2段階（生成器）**：`movegen` を新表現へ書き換える。
居喰いの重複生成と王駒安全フィルタを排除し、`generate_moves` を一本化する。

**第3段階（規則判定）**：`Rules` 構造体を導入し、第12〜15条の判定を6節の仕様で実装する。
成り判定（第16条）の境界を条文テストで固定する。

**第4段階（検証の完成）**：参照実装を着手列挙まで拡張し、ランダムプロパティテストを整備する。
perft 関数を実装して shogiops の参照値と照合し、合格時点の自己 perft 値を回帰値として固定する。
条文対応表を完成させ、6.4節の全項目に回帰テストがあることを確認する。

10節の文献調査は2026年7月29日に完了し、確定した解釈は本書へ反映済みである。
残る論点はローカルルール実装と棋譜実装のマイルストーンで扱う。

## 12. 旧設計からの削除項目

第1版に含まれ、本版で廃止するものを挙げる。

- `MoveKind` と `meta` ビットフィールド、および移動種別ごとのコンストラクタ群。
- `SquareOrNone`（`Option<Square>` の1バイト表現）。捕獲升を保持しないため不要になる。
- `MoveHistory` と `Position::last_move`。
- `RuleSet` トレイトと `dyn` ディスパッチ。
- 疑似合法手と合法手の2段階生成、および全手 make/unmake による王駒安全検査。
- 指し手の8バイト固定サイズ制約。

削除は置き換えと同時に行い、旧表現との互換レイヤーや併存期間は設けない。

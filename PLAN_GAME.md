# 中将棋の対局管理層の設計書（第1版）

## 1. 目的と適用範囲

本書は、中将棋エンジン minase のマイルストーン「対局管理層の成立」の設計を定める。
規則の正は RULES.md（中将棋統合競技規則案 第6版）であり、本書と RULES.md が矛盾する場合は RULES.md を優先する。
対局管理層は、着手の受理と対局の裁定（王駒の捕獲、詰み、合法手なし、反復）を担う層であり、PLAN.md 第3版の13節で `Position` の外側に置くと決めた状態を実装する。

直接の消費者は探索部の土台である。
探索は `Game` を経由せず、`Position` と make/unmake を直接使う前提であり、`Game` は1手ごとの裁定だけを担う。
したがって `Game` に探索向けの性能要件は課さない。

駒枯れ（第22条）、標準規則の千日手帰責（第25条2項）、棋譜入出力、時間管理、待ったは対象外とする。

## 2. 設計判断の一覧

| 判断 | 決定 |
|---|---|
| 既定構成 | 「標準規則＋R1＋E2」と表示し、裁定できない規則の指定は構築時に拒否する（3節） |
| 局面ハッシュ | Zobrist を `Position` の増分更新として実装し、先獅子トリガーはハッシュ外とする（4節） |
| 詰み判定 | 第21条2〜3項を、生成器を再利用した2手読みで実装する（5節） |
| 反復 | 同一局面は射影キーで数え、4回目の裁定は R1 による（6節） |
| R1 の例外条項 | 典拠調査で発動条件を確定してから実装する（6節、8節） |
| 検証 | 条文テスト、Zobrist 整合プロパティ、ランダム自己対局ソークテストで定義する（9節） |

## 3. 既定構成と規則コード

エンジンの既定構成は「標準規則＋R1＋E2」とする（第33条6項の表示による）。

R1 を既定とするのは、標準規則の千日手帰責（第25条2項）が対人運用を想定した規定であり、参照実装がどこにも存在しないためである。
「同じ一群の相手駒を継続して攻撃」「反復を開始した側」という文言の解釈を自前で確定する費用に対し、R1 には Lishogi という参照実装がある。

E2 を既定に含めるのは、駒枯れ見送りの帰結を正直に表示するためである。
第22条を裁定しない実装が標準構成を名乗ると、黙ったフォールバックになる。
既定構成のコード列は、実装が実際に裁定する規則の集合を宣言する。

`Game::new` は、裁定できない構成（標準第25条の帰責、駒枯れ有効）を明示的な Unsupported エラーで拒否する。
E1（実捕獲終局）は詰み裁定を無効化するだけなので、切替可能とする。
RULES.md は連盟準拠の記述を維持し、本書の既定構成は実装の選択である。

## 4. 局面ハッシュ

Zobrist 方式とする。
升と駒コードの組、および手番のキー表を、決定的乱数（手書き xorshift、依存クレートなし）で生成する。
`put_piece` と `remove_piece` および手番反転で XOR により増分更新し、旧値を `Undo` に積む（PLAN.md 5.1節の方針どおり）。
`PositionBuilder` と初期局面はゼロから計算する。

`lion_taken_by_non_lion` はハッシュに含めない。
このトリガーが合法手集合に与える影響は採用ルールで異なるためである（標準と R1 では Some か None の1ビット、将来の L1 と L2 では升の値）。
局面キーは、利用側がハッシュにトリガーの射影を合成して作る（第24条1項c）。

受入条件は、増分値が全再計算と常に一致すること、および make/unmake の往復でハッシュが復元されることである。

## 5. 終局判定

**王駒の捕獲**（第21条1項と5項）：`play` が適用後の王駒の有無で判定する。
獅子が1手で王将（玉将）と太子の両方を取る場合も、適用後の王駒数だけで判定できる。

**合法手なし**（第23条）：手番側の生成手が空なら手番側の負けとする。

**詰み**（第21条2〜3項）：手番側のどの合法手についても、その手が相手の最後の王駒を取らず、かつ適用後に相手が手番側の最後の王駒を取る手を持つ場合、手番側は詰みである。
生成器を再利用した素朴な2手読みで実装する。
対局レベルで1手ごとに走るだけなので、探索向けの高速化はしない。
E1 指定時は詰み裁定を行わず、実捕獲まで対局を継続する。

**投了と引き分け合意**（第21条6項と7項）：着手ではなく `Game` への入力とする。
第24条3項に従い、これらは同一局面の条件に含めない。

## 6. 同一局面と反復

同一局面（第24条）は、盤面、手番、先獅子トリガーの射影がすべて一致する局面とする。
標準規則（L0）の合法手集合はトリガーの有無（Some か None）にのみ依存するため、射影は1ビットとする（第24条2項の「次に認められる合法手が異なる場合は同一局面としない」という基準による）。
Lishogi は局面の同一性に獅子捕獲升の値そのものを含めるが、同サイトは L1 系規則を採用しており升の値が合法手に影響するためである。
L0 では1ビット射影が同じ基準を満たし、この差は L1 を実装する時に射影を升の値へ広げることで吸収する。

R1 の裁定仕様は、2026年7月31日の調査（Lishogi 公開規則と規則ライブラリ scalashogi の実装）で次のとおり確定した。

1. 同一キーの4回目の出現を作る着手が指された時点で自動的に裁定する。原則は引き分けとする。
2. 例外として、最初の出現から4回目までの間の自分の全着手が攻撃的着手であった対局者が一方だけの場合、その攻撃側の負けとする。双方の全着手が攻撃的着手の場合は引き分けに戻る。
3. **攻撃的着手**とは、着手後に相手が王手を受けている（第8条1項。いずれかの王駒を取る手が存在する状態であり、開き王手を含む）、または動かした駒が到達升から相手のいずれかの駒を合法に捕獲できる着手をいう。動かさなかった駒による既存の捕獲脅威は数えない。
4. scalashogi は成り、捕獲、歩兵または香車を動かす着手で反復計数をリセットするが、これらの着手の後に同一局面は再現し得ない（捕獲と成りは不可逆であり、歩兵と香車は後退できない）ため、リセットは結果を変えない最適化である。本実装は全対局を通じたキー計数で同じ結果を得る。

`Game` は射影キーの出現回数と、対局者ごとの連続攻撃着手カウンタを履歴に持つ。
shogiops は反復を裁定しない（裁定はサーバー側 scalashogi の専属である）ため、照合の拠り所は scalashogi の実装とそのテストに置く。

## 7. やらないこと

標準第25条の帰責判定、駒枯れ（第22条）、`Game` の undo と待った、棋譜入出力、時間管理、`make_move_unchecked` と `unmake_move` の公開は、本マイルストーンでは実装しない。
いずれも消費者が現れた時点で追加する。

## 8. 実装順序と発注単位

実装は次の4スライスに分け、S1 から S3 は codex への発注で行う。
各スライスの受入条件は、cargo test の全緑、clippy の無警告、および既存 perft 期待値の不変である。

**S0（調査・並行）**：R1 例外条項の典拠確定。
公開規則の原文引用と実装コードの両方で裏を取り、採用解釈を本書と条文テストに固定する。

**S1（Zobrist 基盤）**：4節の実装とプロパティテスト。

**S2（終局プリミティブ）**：王駒捕獲の検出、王手述語（相手の合法手に最後の王駒を取る手があるか）、詰み判定。

**S3（Game 型）**：構成の検証、`play`、投了と引き分け合意、裁定、履歴。
反復の裁定は S0 の結論を反映するため、S0 完了後に着手する。

S0 から S3 までは2026年7月31日にすべて完了した。
裁定の適用順序は、王駒捕獲、R1 反復、合法手なし、詰み（E1 指定時は無効）の順とした。
反復と他の終局条件が同一手で同時成立することはない（終局性質を持つ局面が反復していれば、最初の出現時点で対局が終わっている）ため、この順序は結果に影響しない。

## 9. 検証

**条文対応テスト**：第20条、第21条、第23条、第24条、および第31条 R1 に境界局面のテストを対応させる。

**Zobrist 整合プロパティ**：ランダムな配置と手順で、増分更新値と全再計算値の一致、および往復でのハッシュ復元を検査する。

**ランダム自己対局ソークテスト**：乱数対局を多数回し、すべてが手数上限内に終局へ到達することを固定する。
手数上限への到達は失敗として数える。
終局理由ごとの検証は、乱数では到達しにくい理由（反復引き分け、攻撃側敗北、合法手なし）があるため、ソークテストではなく作為的な手順による決定的な条文テストに割り当てる。

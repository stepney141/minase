# Descentによる評価関数の強化学習の設計書

## 先に読む要約

本書は、Cohen-Solalが提案した強化学習の手法Descent（[arXiv:2008.01188](https://arxiv.org/abs/2008.01188)）をminaseの評価関数PST（駒の種類と升ごとの点数を足す現行の評価方式）の学習へ適用し、現行の採用PSTより強いPSTが得られるかを確かめる。
現行の学習は、自己対局で実際に現れた局面だけを、その局面の10万ノードの探索値と後続局面の探索値、および対局結果を混ぜた教師値へ近づける方式であり、elmo式と呼ばれる混合教師の系統に属する。
Descentは、自己対局の各手で、評価値が最善の子を終局まで貪欲にたどる探索を繰り返して部分的なゲーム木を育て、対局の終了後にその木の内部節点を、件数を抑えるために一定数を抽出したうえで、木の中のミニマックス値（葉が終局なら勝敗の値、未展開の局面なら静的評価を、手番ごとに最大と最小で逆算した値）へ近づける。
内部節点を学ぶ部分は木の学習（tree learning、線形の評価関数に対する先行研究ではTreeStrapと呼ばれる）であり、降下のたびに終局の値が木へ入るので、終局の勝敗の情報が深い読みの打ち切りを越えて伝わると期待される。
論文の実験はニューラルネットの評価関数と1局60手から150手程度のゲームで行われ、紹介記事の比較は3×3の三目並べだけなので、どちらの結果も中将棋のPSTへそのまま当てはまるとは限らない。
中将棋の対局は中央値で約420手と長く、反復による循環もあるため、最初のフェーズ0で、学習をせずに探索だけを動かし、探索が実際に終局へ届く割合、1局の所要時間、およびメモリ量を実測して、事前に定めた基準で続行を判断する。
続行する場合は、現行の採用PSTを初期値に、一定数の対局を生成するたびにPSTを学習し直す周回を、利用者が決めた計算予算の範囲で繰り返す。
採否は、最終周回のPSTを載せたαβ探索と現行の採用版を、同じ持ち時間で対局させながら優劣を判定する[逐次確率比検定](../guides/sprt.md)の短時間条件STCと長時間条件LTCで決める。
完了条件は、フェーズ0による見送り、学習中の停止規則による見送り、または採否のいずれかが測定記録とともに確定することである。

## 状態

起案。2026年9月26日に起案した。
初期値、対照、計算予算、着手の時期は、2026年9月26日に利用者が決定した（「設計判断」の節）。
着手の条件としていた[探索局面を用いた評価関数の学習](search-aware-evaluation.md)の完了は、2026年9月26日に満たされた（同計画はAとBをともに不採用として完了し、採用PSTは変わっていない）。
次の一手は、同計画の完了後にmasterから専用ブランチを作り、フェーズ0の探索器を実装することである。

## 目的

本書は、「Descentの木の学習でS0から追加学習したPSTは、αβ探索の対局でS0より強い」という仮説を検証する。
ここでS0は、フェーズ0の開始時点でmasterに採用されているPSTと探索のコミットを指し、フェーズ0の実測、駒除去の損失と診断の基準、および採否の比較相手に使う。
本学習の初期値もS0とする。
フェーズ0の後にS0を変える必要が生じた場合は、フェーズ0を新しいS0でやり直す。
この仮説は、次の3つの変化をまとめて含み、個々の寄与を分けない。

1. 教師値が、深さ約6の探索値と対局結果の混合から、終局まで伸ばした部分木のミニマックス値へ変わる。
2. 学習する局面が、対局で実際に現れた局面から、探索木の内部節点全体へ広がる。
3. 自己対局の着手の選び方が、αβ探索の最善手から、Descentの木に基づく確率的な選択へ変わる。

「Descentはelmo式の学習より良い」という比較上の主張は、同じ計算予算で現行の方式を回した対照を要するので、本書では検証しない。
本書の結論は、Descentの学習工程全体がS0を改善したかどうかに限る。

[先行事例の調査](../research/descent-prior-art.md)によれば、線形の評価関数でDescentを使った報告はなく、線形の評価関数に対する木の学習の実証は、αβ探索の木を使うTreeStrapに限られる。
本書は利用者の求めに応じてDescentそのものを試すものであり、フェーズ0で探索が中将棋の終局へ届かないと判定した場合は、αβ探索の木を使うTreeStrapを後続の候補として記録する。

[探索局面の計画](search-aware-evaluation.md)も、探索中の局面を学習へ加える仮説を扱う。
ただし同計画は、局面ごとに独立した1,000万ノードのαβ探索を教師とし、学習局面の半数を置き換える固定の対照実験である。
本書は教師値そのものを自己対局の木から自己参照的に作り、学習を周回で繰り返す点が異なるので、同計画の結果が否定的でも本書の仮説は否定されない。

## 適用範囲

本マイルストーンでは、次の作業を行う。

- 完全化（completion）つきのDescent探索を、ライブラリ内の新しいモジュール `src/descent/` に実装する。
- Descentで自己対局を行い、木の節点を教師データとして出力する生成器 `src/bin/descent_gen.rs` を新設する。
- 対局結果を持たない教師データ形式を定め、Rustの書き出しとPythonの読み込みを実装する。
- 生成と学習を交互に行う周回の駆動器と、再生バッファ（直近の対局のデータを保持して学習に再利用する仕組み）を `tools/train/pst/` に追加する。
- フェーズ0の実現性の実測、本学習、および採否の測定を行う。

次の事項は本書に含めない。

- 論文が対局に使うUBFMs（Unbounded Best-First Minimax with Safe Decision、最良優先の無制限深さ探索で、最も多く選んだ手を指す変種）を、対局用の探索として実装しない。対局は現行のαβ探索だけで行う。Descentで学習した評価関数を使う後続論文の比較でも、子節点の一括評価を使わない条件のチェスではαβ探索がUBFMsを上回っている。
- ニューラルネットの評価関数、GPUでの子節点の一括評価（child batching）、および知識なしの初期化からの学習は扱わない。ニューラルネットの系統は[AlphaZero型探索の計画](alphazero.md)で扱う。
- 論文の層化した再生バッファ（stratified experience replay）は実装しない。論文自身が通常の再生バッファとの差を検出できなかったと述べているためである。
- 論文が提案する他の終局値（得点、移動可能数、駒数）は比較しない。
- αβ探索、時間管理、規則処理、およびPSTの特徴と重み形式は変更しない。

## 依存関係

方式の根拠と先行事例は[Descentの先行事例の調査](../research/descent-prior-art.md)にまとめる。
PSTのモデル、損失、射影、量子化、駒除去の診断は、[評価関数](evaluation.md)、[PSTの補間](tapered-pst.md)、[段階7](strength-stage7.md)、および[PSTの学習手順](../guides/pst-training.md)を引き継ぐ。
棋力の採否は[SPRTの手引き](../guides/sprt.md)に従う。

評価関数の後続計画の順序は[棋力向上段階の設計書](strength-stages.md#評価関数の後続計画の順序)が定めており、本書はその表に含まれていない。
本書は探索局面の計画の完了後に着手する。
同計画で採用されたPSTがあればそれを、なければ従来の採用PSTをS0とする。
着手後は、[相対2駒評価](relative-pair-eval.md)や[段階12](strength-stage12.md)などの変更を途中で取り込まない。

探索局面の計画のブランチは、結果を持たない教師データ形式を別に作っているが、履歴と経路の再生を必須とする重い契約であり、本書の木の節点には不要である。
本書は同ブランチに依存せず、PSTの入力に必要な内容だけを持つ形式を別に定める。

## 設計判断

| 項目 | 決定と理由 |
|---|---|
| 初期値 | S0のPSTを初期値とする追加学習とする。論文の主張は知識なしの初期化から強くなることだが、minaseの目的は棋力であり、S0から始めれば学習の周回を棋力の改善だけに使えるためである。探索用駒価値だけを置いた初期値から学ぶ代案は、論文の設定に近いが、S0に届くまでの周回が余分に必要になるので採らない。 |
| 同じ予算の対照 | 対照を置かず、Descentの工程全体とS0の比較だけを行う。同じ実時間で現行の `selfplay_gen` と先読み教師による追加学習を行う対照は「Descentはelmo式より良い」という主張を検証できるが、本学習とほぼ同じ計算時間が追加で必要になるためである。 |
| 計算予算 | 本学習は同時16対局で48時間を上限とする。48時間は論文の比較実験の1回分の学習時間と同じである。予算はフェーズ0の開始前に `data/descent/manifest.json` に記録し、フェーズ0の実測や学習中の診断を見てから変更しない。 |
| 着手の時期 | [探索局面を用いた評価関数の学習](search-aware-evaluation.md)の完了後に着手する。両計画は教師ラベルと自己対局で測定機を長時間使い、同計画の結果はPSTが探索中の局面から学べるかについての情報を与えるためである。測定機の空き時間にフェーズ0だけを先に行う代案は採らない。 |
| 評価方式 | 現行の鏡映共有つき2端点PST（13,680特徴）に固定する。線形の評価関数に対する木の学習はTreeStrapとして先行研究があり、推論費用を変えずに学習方法の効果だけを調べられるためである。 |
| 探索の置き場所 | ライブラリ内の `src/descent/` に置く。子局面の生成に使う `Position` の着手と取り消しが `pub(crate)` であり、実行ファイル側からは使えないためである。[AlphaZero型探索の計画](alphazero.md)の `src/search/` の再編は起案段階なので、それに依存しない。 |
| 探索の方式 | 論文のAlgorithm 10（完全化と解決済み部分木の停止つきDescent）に従う。完全化のないDescentは最適でない不動点に留まり得ると論文が述べ、中将棋には引き分けがあるので、完全化値cと解決値rの両方を持つ。 |
| 節点の保存 | 転置を共有しない木とし、節点を根からの手順ごとに持つ。反復による引き分け、4,000手の上限、終局値 `MATE − p` はいずれも経路と対局開始からの手数で決まるので、局面キーで節点を共有すると、経路の異なる値を1つの節点に混ぜてしまうためである。論文は置換表を使うが、後続論文の比較でもチェスでは置換表の効果が有意でなかった（[先行事例の調査](../research/descent-prior-art.md)）。局面キーは反復の検出だけに使い、キーには[競技規則](../../RULES.md)第24条が同一局面の条件に含める先獅子の状態が入っている。 |
| 反復の扱い | 根から現在の節点までの経路と対局の履歴に同じ局面キーが同じ手番で現れた節点は、引き分けの終局として扱う。αβ探索の現行の近似と同じであり、貪欲な降下が循環して終わらなくなることも防ぐ。Descentの論文は循環のないゲームを前提としており、公開実装も経路上の再出現での打ち切りや引き分けとしての解決で対処しているので、本書もそれに倣う。 |
| 終局の定義 | 最後の王駒の捕獲は勝ち、合法手がないことは負け（第23条）、経路上の反復と対局開始から4,000手への到達は引き分けとする。詰み（第21条）は、次の手で王駒の捕獲として木に現れるので別に判定しない。R1の攻撃側の負けとE0の駒枯れは木の中では判定せず、αβ探索と同じ近似であることを記録する。対局そのものの終了は、対局管理層 `Game` の正確な裁定に従う。 |
| 終局値 | 勝ちを `MATE − p`、負けを `−(MATE − p)`、引き分けを0とする。pは対局開始からの手数である。これは論文の加算型の深さヒューリスティック（早い勝ちと遅い負けを好む終局値）と同じ形であり、論文の比較で通常の±1より強かった。 |
| 降下の実装 | 再帰ではなく、経路を明示的なスタックに持つ反復で実装する。降下は最大4,000手に達し得るので、再帰ではスタックが溢れる恐れがあるためである。 |
| 1手の探索量 | 実時間ではなく、子局面の評価回数の上限で定める。フェーズ0で2つの候補値を実測し、事前の規則で1つに固定する。評価回数で定めれば、同じシードで生成が再現できるためである。 |
| 着手の選択 | 論文のAthénanの設定に従い、完全化つきの順序分布（ordinal distribution）を使い、その活用率ε′を着手ごとに0以上1以下の一様乱数で引く。順序分布の定義と選んだ理由は「Descent探索の動作」の節に述べる。開始局面は現行の生成器と同じ8〜16手のランダムな開始手順で多様化する。 |
| 学習する節点 | 展開した内部節点と終局の節点を学び、展開していない葉は学ばない。論文の設定どおりであり、葉の値は評価関数自身の出力なので学ぶ情報がないためである。 |
| 節点の出力 | 着手のたびに、新しい根の部分木に含まれない節点を、その時点の値で確定させて出力し、木から解放する。転置を共有しない木では、新しい根の部分木の外にある節点へ以後の降下が届くことはなく、それらの値はもう変わらない。したがって、出力される値は論文の「対局終了時の木全体を学ぶ」処理と同じになり、メモリを1局分の木全体まで溜めずに済む。 |
| 同じ局面の重複 | 転置を共有しないので、別の手順で同じ局面へ至ると、値の異なり得る2件の教師データになる。重複は除かずにそのまま学び、1局あたりの重複件数をフェーズ0で記録する。どちらの値もその手順の木では正しいミニマックス値であり、どちらかを選ぶ規則を足すと実装が複雑になるためである。 |
| 標本の抽出 | 1局から出力する節点は、1局あたりの上限件数を持つreservoir sampling（到来する標本から一定数を等確率で保持する抽出法）で選ぶ。この方法では、出力の時期によらず、1局のすべての節点が同じ確率で残る。上限はフェーズ0の実測から固定する。 |
| 教師データ形式 | 対局結果の欄を持たない新しい形式を定める。PSTの入力に必要な盤面、手番、先獅子の対象升と、節点の値v、完全化値c、解決値r、対局番号、手数、周回番号を持つ。既存のMNSDは対局結果を必須とし、戦術局面と反復局面を除外するので、木の節点には使わない。 |
| 学習の周回 | 論文は対局ごとに学習するが、本書では同時16対局で一定数の対局を生成するごとに学習する周回とする。重みは実行ファイルへ埋め込まれ、学習器はPythonなので、対局ごとの更新は費用に見合わない。生成器は、既に公開されている `Pst::decode` で周回ごとの重みファイルを読み込む。 |
| 学習器 | 既存の `tools/train/pst/` のモデル、射影、量子化、駒除去の損失を再利用する。Rustで線形モデルの学習器を書き直すと、鏡映共有と駒除去の損失を二重に保守することになるためである。既存の学習器は初期重みを駒除去の損失の基準にも使うので、初期重み（前の周回の重み）と基準（全周回でS0）を別々に指定できるようにする。基準が周回ごとに動くと、少しずつの歪みが次の周回の基準に取り込まれて累積するためである。 |
| 損失 | 教師値vとPSTの出力Fから、固定尺度K0で \(\operatorname{BCE}(\sigma(F/K_0),\sigma(v/K_0)) + 1000\,\mathcal L_{\mathrm{removal}}\) を学ぶ。現行の学習と同じ形であり、終局に由来する大きな値は変換後に0または1へ飽和して有界になる。 |
| 再生バッファ | 通常の再生バッファとし、直近の一定数の対局のデータを保持する。層化した再生バッファは適用範囲外とする。 |
| 採否に出す重み | 本学習の最終周回の重みだけを出す。途中の周回を対局結果で選ぶと、採否の測定の前に候補を結果で選ぶことになるためである。 |

## Descent探索の動作

本節では、フェーズ0で実装する探索の動作を、初めて読む人向けに述べる。
木の各節点は、合法手ごとに、子局面の値v、完全化値c、解決値r、およびその手を選んだ回数nを持つ。
完全化値cは、子の部分木で終局まで読み切った結果が勝ちなら1、負けなら−1、それ以外は0であり、解決値rは、その部分木の結果が確定していれば1、していなければ0である。

1回の降下は、根から始めて次の手順を繰り返す。
まず、未展開の節点に着いたら全合法手を生成し、各子局面をPSTで評価してvとし、王駒を取る手の子は終局値で埋める。
次に、解決していない子のうち、辞書式順序で最善の子へ降りる。
ここでの辞書式順序は、値cとvを節点の手番側から見た値へそろえたうえで、cの大きい子を優先し、cが等しければvの大きい子を優先する比較である。
終局に着いたら、経路を逆にたどって各節点の (c, v) を子の最善値で、rを子の解決状態で更新する。
降下は、終局に着くか、全ての子が解決した節点（解決済みの節点）に着いたところで終わる。
解決済みの節点は部分木の結果が確定しているので、それ以上降りずに、その (c, v) をそのまま経路へ逆伝播する。
展開した直後に全ての子が終局だった節点も、その場で解決済みとなり、降下はそこで終わる。
したがって、1回の降下ごとに、終局の値か、終局から確定した値が1つ木へ入る。
ただし、根の値が終局に由来するとは限らない。
降りた子が負けと分かれば、親の最善値は、まだ展開していない兄弟の静的評価へ移り得るからである。
このため、出力する節点ごとに、その値がどの葉から来たかを記録する。
葉の種類は、王駒の捕獲と合法手なしによる勝敗の終局、反復と手数上限による近似の引き分け、および未展開の局面の静的評価の3種類とする。
評価回数の上限は、降下を始める前にだけ確かめる。
始めた降下は、上限を超えても終局まで続けて完了させ、未完了の降下を残さない。
根が解決するか、降下を始める時点で1手の評価回数が上限に達していれば探索を終え、順序分布で着手を選ぶ。
ただし、手番側から見てc=1の子（勝ちが確定した子）があればそれを指し、c=−1の子（負けが確定した子）は、c=−1でない子が1つでもある限り選ばない。

順序分布は、候補の手を上の辞書式順序で最善から並べ、先頭から順に採否を決める。
残りの候補がk手のとき、先頭の手を確率 \((\varepsilon'(k-1)+1)/k\) で選び、選ばなければ次の手へ進む（論文のAlgorithm 14）。
この確率は、ε′=1なら1（常に最善手）、ε′=0なら1/k（残りから一様に選ぶ）となり、その間を線形に補間する。
したがって、選ばれる確率は評価値の差の大きさではなく順位だけで決まる。
論文は、この分布がε-greedyやsoftmaxより調整しやすく、勝率もわずかに高かったと述べる。

論文は同値の子を乱数で選ぶ。
本書では再現性のため、乱数を対局番号と手数から導くシードで生成する。

## 計算量の見積り

着手前の桁を次のとおり見積もる。
[係数の表の速度測定](../measurements/bench-spsa-params-nps.md)の深さ8のNPS（1秒あたり探索ノード数）は約212万であり、子局面の評価1回がαβ探索の1ノードと同程度の費用だと仮定する。
子局面の評価は着手の適用、PSTの評価、および取り消しからなり、展開ごとに合法手の生成が1回加わる。
αβ探索の1ノードも同じ処理に置換表の参照と手の順序付けを加えたものなので、桁は同じと見込む。
[FM候補の診断](../measurements/fm-candidate-diagnostics.md)の代表6局面の合法手数は34手から96手なので、1手の評価回数の上限を100万回とすると、1手は約0.5秒、中央値420手の対局は1局約3.5分になる。
同時16対局なら1時間に約270局であり、48時間では約1万3,000局になる。
ただし、順序分布による確率的な着手は対局を長くし得るので、この見積りはフェーズ0の実測で置き換える。
メモリは、測定機の実メモリ30 GBのうち同時16対局で半分以内に収めるため、1ワーカーあたり最大常駐メモリ900 MiB（16ワーカーで約15 GB）を上限とする。
展開した節点1つは、合法手ごとに着手、v、c、r、nを持つので1手あたり約16バイト、合法手70手で約1.1 KBを要し、この上限は約80万節点に当たる。
木に残るのは現在の根の部分木だけだが、転置を共有しないので同じ局面が重複して残る。その節点数はフェーズ0で実測する。

## 実装フェーズ

### フェーズ0　実現性の実測

`src/descent/` の探索、木、終局の判定、節点の出力、および `descent_gen` の自己対局を実装する。
開始前に、S0のコミット、48時間の予算、およびフェーズ0の続行基準をmanifestへ固定する。
S0の重みを固定し、学習はしない。
1手の評価回数の上限を10万回と100万回の2通りにして、それぞれ64局（32ペアの開始手順）を生成し、次の量を記録する。

- 降下1回の長さの分布と、1手あたりの降下回数を記録する。
- 降下の終わり方を、王駒の捕獲、合法手なし、反復、手数上限、および解決済みの節点に分けた割合として記録する。解決済みの節点で終わった降下は、その節点の完全化値cが±1なら勝敗、0なら引き分けとして下位分類する。
- 出力した節点の値の由来を、勝敗の終局、近似の引き分け、静的評価に分けた割合として記録する。
- 1手ごとの実際の評価回数と、上限からの超過量を記録する。
- 1局の手数、所要時間、出力節点数、同じ局面の重複件数、およびワーカーの最大常駐メモリを記録する。
- 対局の終わり方を、対局管理層 `Game` の裁定理由（王駒の捕獲、詰み、合法手なし、反復、駒枯れ）と、生成器が独自に打ち切る4,000手の上限とに分けて記録する。4,000手目の着手で `Game` が終局を裁定した場合は、`Game` の裁定を優先する。

評価回数の上限は、次の続行基準をすべて満たす候補のうち小さいほうに固定する。

1. 64局の全降下を合計した件数の50%以上が、王駒の捕獲、合法手なし、またはcが±1の解決済みの節点で終わる。
2. ワーカーの最大常駐メモリが900 MiB以下である。
3. 同時16対局で、48時間の予算の80%にあたる38.4時間に、生成だけで1,000局以上を生成できる。残りの20%は周回ごとの学習の時間に充てる。

基準1は、論文の機序である「降下のたびに終局の勝敗が木へ入る」ことが中将棋でも働くかを判定する。
降下の過半が反復や手数上限という近似の引き分けで終わると、木に伝わる終局値の大半が、勝敗ではなく本書の近似が与えた0になる。
過半を境にしたのは、勝敗による終局値が近似の引き分けより多いことを最低限の条件とするためであり、この割合は評価回数の上限ではなく降下の性質で決まるので、2つの候補に同じ基準を使う。
基準2はメモリの節の上限であり、基準3は、論文のArimaaの学習（6,317局）やHexの13×13の学習（5,288局）と同じ千局の桁の対局数を予算内に確保する条件である。
これらの数値は計算を投じる前に固定する運用上の基準であり、統計的な保証ではない。

出力節点の値の由来の割合は、続行の判定には使わず、学習結果の解釈に使う診断とする。
辞書式順序で値を逆算する木では、値が勝敗の終局に由来する節点は、完全化値cが±1の節点、つまり木の中で勝ちか負けが確定した節点と一致する。
対局の序盤や中盤の節点がそのように確定することは構造上まれなので、この割合に下限を課すと、仮説を試す前に計画を止めてしまうおそれがある。
論文の主張も、終局の値がより速く他の節点へ伝わることと、生成される局面が終局に近いことであり、教師値の多くが終局に由来することではない。
ただし、探索値だけを教師とした[混合比λ=1.0の候補](teacher-mixing-ratio.md)は短時間測定で得点率28.0%と大きく負けたので、採否で負けた場合には、この割合を自己参照の教師の危険を示す記録として参照する。
どちらの候補も基準を満たさなければ、本書を見送りとして完了し、測定記録に理由を残す。
記録は `descent-feasibility` とする。

### フェーズ1　学習の周回

教師データ形式のPython読み込み、再生バッファ、周回の駆動器を実装する。
周回の大きさ、再生バッファの対局数、1周回の更新回数、学習率、1局あたりの出力上限を、フェーズ0の実測から固定し、`data/descent/manifest.json` に記録する。
学習率と駒除去の損失の係数は、現行の学習手順の0.03と1000から始める。
[1エポックの更新回数の教訓](../lessons/check-steps-per-epoch-before-training.md)に従い、固定した設定で本学習全体の更新回数が数千回以上になることを確認する。
4周回の試運転で、重みの読み込み、書き出し、生成器での再読み込み、学習損失の低下が一巡することを確かめて完了とする。

### フェーズ2　本学習

48時間の予算で周回を繰り返す。
周回ごとに、訓練損失、保持した検証用の節点に対する損失、S0に対する重みの差、および駒除去の診断を記録する。
10周回ごとに、S0と固定10万ノードで50ペアを対局させて進捗を記録する。
この進捗の対局は診断であり、途中の停止、延長、重みの選択には使わない。

次の場合は本学習を停止する。

- 非有限値が発生した場合は、不具合を修正して同じ設定で最初からやり直す。
- 周回の重みが、S0を基準とする[PSTの学習手順](../guides/pst-training.md)の既存の駒除去診断に合格しなかった場合は、その周回で停止し、原因を調べる。設定を変えずに解消できなければ見送りとする。既存の診断は、S0と符号が異なる除去の評価差を、改善の向きであっても拒否する。
- 予算に達した場合は、最終周回の重みを候補とする。

記録は `descent-training` とする。

### フェーズ3　採否と現行設計の更新

最終周回の重みを `nets/pst.bin` に置いた候補コミットを、S0に対して[標準手順](../guides/sprt.md)のSTCとLTCで測る。
測定名は `descent-pst-stc` と `descent-pst-ltc`、基本シードは191000926と192000926とし、準備時に既存の実行記録と重複しないことを検査する。
採用した場合は、[評価関数](evaluation.md)と[PSTの学習手順](../guides/pst-training.md)の現行の教師と学習方法を更新する。

## 検証

検証の期待値は論文のアルゴリズムと[競技規則](../../RULES.md)から定め、実装の出力をそのまま正解にしない。

| 対象 | 独立した期待値と確認事項 |
|---|---|
| 完全化 | 論文の図3と同じ形の小さな木で、未解決の高い値より解決済みの勝ちを選ぶ。 |
| 解決値 | 全ての子が解決した節点は解決し、勝ちの子が1つあれば他の子が未解決でも解決する。解決済みの節点に着いた降下はそれ以上降りず、展開直後に全ての子が終局だった節点で降下が終わる。 |
| 終局の判定 | 手で作った局面で、最後の王駒の捕獲、合法手なし、経路上の反復、手数上限の各終局値を確認する。王将と太子の両方を持つ側は、一方を取られても終局しない。 |
| 視点 | 先手と後手の節点で、最善の子の選択と値の符号が手番側から正しいことを確認する。 |
| 節点の出力 | 同じ対局を、着手ごとの出力と対局終了時の一括出力の2通りで実行し、出力された節点の値が一致することを確認する。別の手順で同じ局面へ至る転置を含む局面列も用い、2つの経路の節点が別々に保たれることを確認する。 |
| 再現性 | 同じシードと評価回数の上限で、同じ対局と同じ出力が得られる。 |
| 教師データ | Rustの書き出しとPythonの読み込みで、盤面、先獅子、値が往復で一致し、PSTの評価もRustとPythonで一致する。 |
| 学習 | 固定した小さなデータで、損失が下がり、射影後の重みが保存範囲に収まる。 |

実装時には `cargo test`、`cargo clippy -- -D warnings`、`cargo fmt --check` と、変更した学習器の単体テストを通す。
計画書の作成だけを理由に生成や対局測定を実行しない。

学習した重みを対局へ出す前に、[検証損失が駒価値の歪みを隠す教訓](../lessons/validation-loss-hides-material-distortion.md)に従い、初期配置から駒種を1つずつ除いた局面の評価をS0と比べる。
学習が停滞した場合は、[学習曲線を先に確認する教訓](../lessons/check-learning-before-blaming-data.md)に従い、データ量を増やす前に学習器の欠陥を疑う。

## 完了条件

- フェーズ0の見送り、本学習の停止規則による見送り、または候補の採否のいずれかが、事前の規則に従って測定記録とともに確定している。
- 採用する場合は、候補がS0に対するLTCで `H1` となり、エンジン異常、時間切れ、拒否着手が0件である。
- 見送りまたは不採用の場合は、否定された仮説の範囲（PSTへの適用、評価回数の上限、予算）と、未検証の範囲（ニューラルネットの評価関数、UBFMsによる対局）を区別して記録している。
- 現行設計、本書の要約と状態、ROADMAPが結論と一致している。

## 参考資料

- Q. Cohen-Solal, “Learning to Play Two-Player Perfect-Information Games without Knowledge,” [arXiv:2008.01188](https://arxiv.org/abs/2008.01188)（v5、2025年5月7日）。Descent（Algorithm 9と10）、木の学習（Algorithm 3）、完全化（第5節）、終局値のヒューリスティック（第6節）、順序分布（第7節）の出典である。
- J. Veness, D. Silver, W. Uther, A. Blair, “Bootstrapping from Game Tree Search,” NIPS 2009。線形の評価関数に対する木の学習（TreeStrap）の出典であり、[一次資料の調査](../research/relational-evaluation-primary-sources.md)が既に確認している。
- [tech-streetの紹介記事](https://www.tech-street.jp/entry/2024/07/16/123852)（2024年7月16日）は、三目並べでDescentとelmo式の学習を比べ、Descentの正解率が最大98%、elmo式が約80%だったと報告する。
- [Descentの先行事例の調査](../research/descent-prior-art.md)は、公開実装、後続論文、および長いゲームでの報告をまとめる。

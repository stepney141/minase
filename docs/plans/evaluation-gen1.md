# 評価関数の世代反復の設計書

## 実施状況

2026年8月27日に本設計書を起案した。
前提となる評価関数マイルストーン（plans/evaluation.md）は2026年8月26日に完了し、学習PSTを採用する一方で、世代0の100万局面で学習したP型NNUEは学習PSTに対する固定ノードGSPRTで`H0`となった。
GSPRTは、自己対局の勝敗から2つのエンジンの強さの差を逐次判定する統計検定であり（docs/sprt.md）、`H1`は候補が有意に強いという判定、`H0`は「elo=5の改善があるとは言えない」という判定、`pending`は判定保留を表す。
同書は敗因を教師不足と判定し、生成規模を1桁上げる計画と世代反復を次期マイルストーンの起案材料として残した。
起案時に利用者は、複雑な評価関数は探索速度を下げ、総合では弱くなるのではないかという懸念を示した。
本書はこの懸念を設計判断第6の測定手続きとして取り込み、固定ノードと時間制御の両方の結果を診断指標つきで記録したうえで、採否は時間制御GSPRTで決める。

同日、起案稿に対するcodexの批判的レビューは21件の指摘を返した。
事実誤り3件（世代0データの容量と特徴観測回数、HaChuの規則列のP0欠落、アキュムレータの型）を訂正した。
設計の修正は次のとおりである。
現行の学習器は全局面の特徴を一括でGPUへ置くため1,100万局面ではメモリに収まらず、バッチごとに特徴を作るストリーミング方式への変更を必須の前提作業に加えた。
ランダム着手の後の対局結果が注入前の局面の教師値を汚すため、記録を最後の注入より後に限る契約に改めた。
固定ノードGSPRTを「知識だけ」の測定とする因果的な解釈と、`H0`を「利得なし」と読む判定表を改め、採否を時間制御GSPRTだけで決め、固定ノードは診断指標と併記する形にした。
勝率尺度Kは世代別に推定して差を確認し、検証分割は来歴に基づく安定した対局IDで行うことにした。
測定用コミットは採用済みの基準を直接の親にする構成を明記し、NNUEの復元は設計監査後の探索APIを維持する契約として具体化した。
参考測定は学習と並行させず、SIMD化の発動条件にプロファイルの確認を加え、`unsafe_code = "forbid"`との両立を発動時に設計することにした。
上限4回と80手のパイロット判定基準は、レビューの提案を簡略化し、除外率、終局の内訳、探索値の分布、局面の重複率の4項目で世代0と比べる形にした。

第2稿に対するcodexの再レビューは6件の指摘を返し、すべて反映した。
時間制御で`pending`となった候補を不採用や高速化の判断に使っていた矛盾を正し、判定保留として docs/sprt.md の契約どおり再測定または見直しへ戻す形にした。
到達深さと静止探索ノードの比率は`match_runner`が`info`行を捨てるため現行の構造では取れないので、診断指標をbench NPSと評価呼び出しの単体計測に絞った。
世代別の教師Kとモデル出力のKを分ける学習器の変更を必須作業に加え、5%の分岐をなくして常に世代別の教師Kを使うことにした。
Kは探索値（センチポーン）を勝率へ写す尺度であり、探索値をKで割った値をシグモイド関数に通して勝率とする（「教師値と損失」は前マイルストーンの設計書に詳しい）。
除外率の分母を記録対象の局面に定め、パイロットの合否を注入なしの対照パイロットとの数値差で定義し、生成時間の見積りをパイロット前の仮値と明記した。

2026年8月27日にフェーズ1（学習器のストリーミング化とランダム着手の注入）へ着手し、同日に実装を完了した。
実装はcodexへ委任し、`selfplay_gen`のランダム着手の注入をコミットc77f2ef、学習器のストリーミング化をコミットdd46d1eで確定した。
起案時の設計書とROADMAP.mdの更新はコミットb2f3487である。

コミットc77f2efは、`generate`に必須引数`--random-moves`（0〜80）を加え、対局ごとに序盤ランダム化とは独立のシード（`derive_seed(game_seed, 1)`）から注入回数と手数を決める。
注入手数は序盤終了後80手の範囲から部分Fisher–Yatesで重複なく選び、記録は最後に予定した注入手数の次の局面から始める。
境界より前の教師探索は探索回数に数えるが記録対象にも除外にも数えず、除外率の分母は記録対象の局面数（サマリの`recordable_positions`）に改めた。
サマリには注入の予定回数と実施回数、オフセットの10手幅ヒストグラム、記録局面の探索値の平均・母標準偏差・分位点（p01からp99）、対局をまたいだ探索キーの重複率を加えた。
決定性、回数と手数の範囲、記録の境界、上限0の計画、CLIの範囲検査、分布の計算、重複の計数を8件のテストで固定し、`cargo test`は498件すべて通過した。
注入0の出力は、変更前コミットb2f3487のバイナリと変更後のバイナリで16局（シード20260827、5,000ノード、並列4）を生成してバイト単位で一致した。
両者はヘッダの生成コミット欄も同じ（変更後は同じHEADの作業ツリーから`--allow-dirty`で生成）であり、サマリの差分は追加行だけである。

コミットdd46d1eは、`mnsd.py`に複数ファイルをメモリマップのまま保持する`Dataset`を加え、全件を複製して連結する旧`load_records`を削除した。
同じファイルの二重指定とシードの重複は拒否し、世代はヘッダのネット検査和の出現順で番号を付ける。
検証分割は`hash64(seed, game) % 20 == 0`で決め、`hash64`はsplitmix64の混合をnumpyのu64演算で計算する。
`train_pst.py`は、エポックごとの順列とバッチごとの鏡映選択（盤面を左右反転して学習に使うデータ拡張を標本ごとに選ぶこと）の乱数呼び出し順を旧実装から変えずに、バッチの盤面を`gather`（大域番号の列に対応するレコードを複数ファイルのメモリマップから集める関数）で読んで特徴と教師値をCPUで作りデバイスへ送る。
教師値の変換には世代別に推定した教師Kを使い、モデル出力のKは`--k`で与える。
エポック0を含む検証損失（学習に使わない検証用の対局で測った損失で、小さいほど予測が良い）最良の重みを保存し、世代別の検証損失、局面/秒、特徴の観測回数の分位点と未観測数、最大常駐メモリ、CUDAの最大VRAMをログに出す。
`test_train_pst.py`の4件（重複ファイルとシードの拒否、分割のファイル順序不変性、`gather`の順序保持、世代別教師KとモデルKの分離）は通過した。

設計書の検証項目「世代0データで学習した結果が現行の`nets/pst.bin`と量子化誤差（浮動小数点の重みを整数化することで生じる評価値の差、合格条件は平均2センチポーン以下）の範囲で一致する」は、検証分割の方式を変えたため成立しない。
新しい分割で訓練集合が5%入れ替わり、最良エポックの規則も変わるので、同じ重みには収束しない。
codexの計測では、世代0データで再学習した重みと`nets/pst.bin`の評価差は検証標本1万局面で平均90.3センチポーン、重みの差は平均35.9センチポーンであった。
代わりに次の3点で学習器の同値性を確認した。
第1に、`nets/pst.bin`を新しい分割の検証集合で評価した損失は、学習器の出力0.412888331に対して、`feature_indices`以外を共有しない独立のnumpy参照実装で0.412888327であり、float32とfloat64の差の範囲で一致した。
第2に、同じシードと引数の2回のCPU学習は出力ファイルがバイト単位で一致した。
第3に、新しい分割で`nets/pst.bin`の検証損失（0.41289）が再学習の最良値（0.41463）より低いのは、`nets/pst.bin`が旧分割で学習しており新しい検証対局の約95%を訓練に使っていたためであり、学習器の欠陥を示さない。
検証損失の水準が前マイルストーンの0.3946から0.4146へ動いたのも同じ分割の変更による。
評価差90センチポーンは、Adamの1更新あたりの重みの移動が勾配の大きさによらず学習率3の程度であり、1エポック58更新のたびに重みが大きく動くことと整合する。

ストリーミング学習の計測は次のとおりである。
codexのCPU実行（世代0の100万局面、10エポック、学習率3）は1エポック約17万局面/秒、最大常駐メモリ969MB、最良エポック1（検証損失0.414634）、量子化誤差は平均0.470センチポーン、最大1.624センチポーンであった。
測定機のGPU（RTX 3060 Ti）で`nets/pst.bin`を初期値に1エポック実行した結果は、240,550局面/秒、最大常駐メモリ1.37GB、最大VRAM 74.9MB、全体の経過6.8秒であった。
特徴の観測回数は、訓練95万局面で未観測5,181個、観測済みの中央値219回、p05は3回、p95は10,932回、平均6,143回であり、世代0の観測回数の偏りは大きい。
GPUの240,550局面/秒から、1,100万局面では1エポック約46秒（1,100万÷240,550）、10エポックで約8分と見積もる。
常駐メモリはメモリマップした1.8GBと添字配列で数GBにとどまる見込みであり、GPUへ置くのはバッチだけなのでVRAMは増えない。

ランダム着手の注入のパイロットは、コミットdd46d1eで同じシード20260827、128局、100,000ノード、16並列を、上限0（対照）、4、2の3条件で行った。
対照は記録34,263局面、詰み終局99局（77.3%）、手数上限600での破棄21局、除外率は捕獲または成り16.43%、詰み帯0.73%、再出現0.12%、探索値の平均−57、母標準偏差2,119、重複率0、経過258秒、記録133局面/秒であった。
上限4は予定252回・実施246回の注入があり、記録26,339局面、詰み終局98局（76.6%）、破棄24局、除外率は16.44%、0.85%、0.11%、探索値の平均−96、標準偏差2,822、重複率0、記録111局面/秒であった。
上限2は予定136回・実施135回、記録28,220局面、詰み終局93局（72.7%）、破棄25局、除外率は16.84%、0.75%、0.15%、探索値の平均−83、標準偏差2,588、重複率0、記録114局面/秒であった。
本書「ランダム着手の注入」の節で定めた合否基準（理由別の除外率と詰み終局率は対照との絶対差5ポイント以内、探索値の標準偏差は相対差20%以内、重複率は対照以下）のうち、理由別の除外率と詰み終局率の絶対差（最大でも上限2の詰み終局率−4.7ポイント）および重複率は両条件とも満たしたが、探索値の標準偏差の相対差は上限4で+33.2%、上限2で+22.2%であり、いずれも20%以内を満たさない。
同節が定める手順（満たさなければ上限2で再測定し、それでも満たさなければ注入を見送る）に従い、世代1の生成ではランダム着手の注入を見送り、世代0との併用だけで分布収縮に対処する。
標準偏差の増加には、記録の開始が最後の注入より後に遅れることで序盤から中盤への移行部（探索値が0付近に集まる）が記録から外れるという構造的な要因が含まれるので、この基準は分布の歪みと記録範囲の変化を区別できない。
注入を再検討する場合は、記録範囲を揃えた比較（対照側も同じ手数から記録する）で基準を作り直す必要がある。

対照パイロットは、生成器が学習PST（コミット7e13888の重み）になったことで世代0（v0で生成）と様相が異なる。
1局の平均手数は435手（世代0は293手）、手数上限600での破棄は16.4%（世代0は2.8%）、詰み終局は77.3%（世代0は93.5%）、捕獲または成りの除外率は16.4%（世代0は23.7%）、記録速度は133局面/秒（世代0は171局面/秒）である。
設計判断第1の見積り（約170局面/秒で約16時間）は世代0の速度に基づいており、実測の133局面/秒では1,000万局面に約21時間（10,000,000÷133÷3,600）を要する。
破棄された対局の探索時間は生成量に寄与しないが、手数上限は設計判断第9（教師値の定義、除外規則、教師探索のノード数、規則セットといった生成条件を本マイルストーンでは変えない方針）に従って600のまま変えず、次期候補の検討事項として残す。

フェーズ2の本生成は2026年8月27日8時39分（JST）に開始した。
対照パイロットの1局あたり記録268局面と1局あたり2.0秒（16並列）から、7,500局×5回（シード3、4、5、6、7）で約1,005万局面、約21時間と見積もる。
コマンドは各回`selfplay_gen generate --output data/gen1-s<seed>.bin --games 7500 --seed <seed> --nodes 100000 --concurrency 16 --random-moves 0`であり、生成中に本ツリーで文書編集やコミットを行ってもヘッダの生成コミットが変わらないよう、コミットdd46d1eを固定したgit worktreeでビルドしたバイナリを実行する。
注入0を指定したときに生成器が通るコードは変更前と同じであり出力もバイト単位で一致しているので、後述のcodexレビューで注入だけに関わる実装に修正が入っても、注入0で生成した世代1データは影響を受けない。

コミットc77f2efとdd46d1eに対するcodexの敵対的レビューは、重大な欠陥なしで3件を指摘した。
主要な経路がテストで固定されていないという指摘（並列度を変えた書き出しの一致、最良エポックの同点規則、世代別の教師K、通常特徴の観測、量子化標本の所属）と、記録対象が0件のときの除外率を`0.000000`と表示する指摘は採用し、コミットbfeb1aaで修正した。
書き出し側は任意の到着順で対局を受け取る関数`merge_completed_games`に切り出し、到着順を変えても出力バイト列・統計・度数表・重複数が一致することと対局番号の欠落の拒否をテストで固定した。
学習器は合成した2世代のデータでCLIの訓練経路全体を通すテストを加え、各性質について対応する変異（`<`を`<=`へ、量子化標本を訓練集合へ、観測回数に鏡映を含める、教師Kを世代0に固定、到着順のまま書き出す）がテストで検出されることを確認した。
`XorShift64::index`が`next() % length`で選ぶため一様分布から約2^-64ずれるという指摘は採用しない。
剰余の偏りは1回の抽選あたり最大でも2の64乗分の1であり対局の統計に現れる大きさではない一方、乱数生成器を変えると序盤ランダム化と`match_runner`の開始局面が変わり、世代0データと既存の測定の再現性が失われるためである。

フェーズ2の生成と並行して、2026年8月27日にフェーズ4のNNUEの復元を分岐`nnue-gen1`（masterのコミット932b064から分岐）で行った。
設計の親子関係（NNUEのコミットは学習PSTの採否後のコミットを親にする）を守るため、masterには取り込まず、フェーズ3の判定後に親を付け替える。
復元はcodexへ委任し、`nnue-residual`の`src/eval/nnue.rs`、`tools/train/nnue_net.py`、`tools/train/train_nnue.py`を取り出して、設計監査後の契約（`&Network`と`Arc<Network>`の明示的な受け渡し、`Undo`の非公開、両視点の特徴関数の`pub(super)`、`XorShift64`と`PieceCode::new`の新しいAPI）へ適合させ、`train_nnue.py`を`Dataset`によるストリーミングと世代別の教師Kへ改めた。
`cargo test`は524件が通過し、`test_train_nnue.py`はエポック0の採用と世代別Kを固定する。

復元の過程で、`nnue-residual`の`nets/nnue.bin`が学習済みネットではないことが分かった。
このファイルを埋め込んだ探索は、初期局面からの深さ3で200万ノードを超えて返らず、`match_runner`の統合テストは深さ1の1手が応答上限の120秒に達した。
差分更新と全再計算の一致（60手のランダム対局で不一致0）と静的評価値の健全性（線形項±10センチポーン）を確認したうえで探索を計数すると、30万ノードのうち静止探索が281,609回、到達plyは80であり、静止探索のノードでは750〜1,000センチポーンの駒を取り合う途中でもstand-pat（静止探索でその局面で捕獲をやめた場合の評価値。相手の許容上限βに達すれば探索を打ち切る）が−111から+20の範囲にとどまっていた。
ネットファイルを直接調べると、残差線形項は平均絶対値10センチポーンで`nets/pst.bin`と無相関（相関係数−0.01）、Kは807.0であり、`nnue_net.py random`の初期化分布と一致する。
評価が駒得に反応しないためstand-patによる打ち切りも、delta pruning（stand-patに捕獲する駒の価値と余裕分を足しても下限αに届かない捕獲手を省く枝刈り）も一切効かず、捕獲手のDAG全体を列挙していたのであり、復元コードにも残差構造にも欠陥はない。
したがって分岐の埋め込みネットは、`train_nnue.py train --epochs 0`で作る「線形項＝`nets/pst.bin`の重み、NNUE側の出力層＝0」のエポック0ネットに差し替えた。
このネットの評価は学習PSTと厳密に一致し、初期局面からの探索は深さ3、4、5でそれぞれ4,086、11,860、34,803ノード、評価52、21、62とmasterの学習PSTに完全に一致した。
これはNNUEの推論と差分更新の経路が学習PSTの整数評価を正確に再現することの回帰確認でもある。
codexが一時的に縮めていた既存テストの深さと対局長は元へ戻し、元の設定で全テストが20秒で通ることを確認した。
只取りの獅子を優先するテスト（D7-SRCH-04）は、ランダム初期化ネットでは深さ1で王の移動を選んで失敗したが、エポック0ネットでは元の主張のまま通る。
学習済みネットを埋め込む段階でこのテストが失敗すれば、疎な盤面での評価の頑健性を示す診断情報として扱う。

この発見は前マイルストーンの記録の読み方にも関わる。
`nnue-residual`のコミット510fcdeは残差線形版の学習曲線（2エポック目で最良、以後過学習）を根拠に不採用となったが、そのコミットに埋め込まれたネットは学習結果ではない。
残差線形版の対局測定は行われておらず、世代1で初めて残差線形版のNNUEを対局で測ることになる。
benchのNPSは、16並列の生成と同時に測った参考値として669,500（深さ5、3回の中央値、ノード数は3回とも6,479,633で一致）であり、正式な診断指標は生成の停止後に測り直す。
ストリーミング版`train_nnue.py`のGPU計測（世代0の100万局面、バッチ4,096、学習率0.01、1エポック、16並列の生成と同時）は、105,984局面/秒、最大常駐メモリ3.0GB、最大VRAM 197MB、全体の経過13秒であった。
1,100万局面では1エポック約104秒、20エポックで約35分と見積もる。
エポック1の検証損失（0.412918）はエポック0（0.412888）をわずかに上回り、世代0の100万局面ではNNUE部分の学習が学習PSTを改善しないという前マイルストーンの観察と整合する。

2026年8月27日16時32分（JST）にシード4の2回目が完了した時点で、そのサマリがシード3の1回目とほぼ同一（破棄1,012局、詰み6,184局、記録のある対局6,463局、探索値の最小と最大も一致）であることに気づき、レコードを直接比べて対局の重複を確認した。
シード4の第n局は、盤面と探索値の全レコードがシード3の第n＋1局と完全に一致する。
原因は`rng::derive_seed(base, n) = splitmix64(base + n)`にあり、隣接する基本シードは1局ずれた同じ対局シード列を生む。
同じ理由で世代0の`gen0b.bin`（シード2、420局）は`gen0.bin`（シード1）の第2〜421局の完全な複製であり、世代0の1,002,722局面のうち78,307局面は重複である。
この重複は同一対局内の再出現しか除外しない生成器と、ファイルのシードと対局番号で分ける検証分割の双方をすり抜け、検証集合を汚染していた。
フェーズ1に記録した世代0の検証損失（0.4129など）はこの汚染を含む値として読む。

対処は次のとおりである。
連鎖を停止し、シード3、4の出力と実行中だったシード5の部分出力を破棄した（シード3の対局シード列は世代0のシード1の第3局以降と序盤を共有するため、世代1として残さない）。
生成は同じコミットdd46d1eのまま、互いに7,500局以上離れた基本シード100000、200000、300000、400000、500000で16時36分に再開した（`--games 7500 --nodes 100000 --concurrency 16 --random-moves 0`、出力`data/gen1-s<seed>.bin`）。
`gen0b.bin`は完全な複製なので、以後の学習と検証には`gen0.bin`（924,415局面）だけを世代0として使う。
`derive_seed`の混合方式そのものの修正は、`match_runner`の開始局面と過去の測定シードの再現性に関わるため本マイルストーンでは行わず、次期の検討事項として利用者の判断に委ねる。
生成の所要時間は1回約4.0時間（シード3の実測14,305秒、記録143.5局面/秒、2,052,694局面）であり、5回の完了は2026年8月28日13時ごろの見込みである。

フェーズ2の世代1データは2026年8月28日13時16分（JST）に完了した。
5回の生成（シード100000、200000、300000、400000、500000、各7,500局、100,000ノード、16並列、注入0、生成コミットdd46d1e）の記録局面はそれぞれ2,033,330、2,021,485、2,035,797、2,017,014、2,030,770の合計10,138,396局面（ファイル合計1.62GB）であり、目標の1,000万局面を満たす。
所要はそれぞれ14,123秒、14,089秒、18,084秒、14,042秒、14,060秒で、3回目だけは他の負荷のため5.0時間を要したが生成条件は同じである。
手数上限600での破棄は1,019〜1,094局（13.6〜14.6%）、詰み終局は6,131〜6,177局（81.7〜82.4%）、記録のある対局は6,382〜6,452局、探索値の標準偏差は2,139〜2,195で、5回の統計は互いに近く重複はない。
全ファイルは`inspect`の検査を通過し、要約を`data/gen1-s<seed>.inspect.txt`に保存した。

フェーズ3の学習PSTの再学習は同日13時17分から行った。
世代0の`gen0.bin`（924,415局面）と世代1の5ファイルを合わせた11,062,811局面のうち、訓練は10,520,846局面、検証は541,965局面である。
教師Kは世代0が804.03、世代1が1,099.68であり、混合Kは1,072.65である。
世代1の探索値は同じ勝率に対して世代0より約37%大きく、生成に使った評価関数によって探索値の尺度が変わるという設計判断第2の想定が実データで確認された。
学習は現行の`nets/pst.bin`を初期値に学習率3、バッチ16,384、10エポック、モデル出力K=1,072.65で行い、GPUで1エポック約39秒（272,607局面/秒）、全体6分51秒、最大常駐メモリ3.1GB、最大VRAM 509MBであった。
検証損失は全体でエポック0の0.512759からエポック4の0.509899へ下がり（以後は0.5099〜0.5101で横ばい）、世代別では世代0が0.419890から0.418636、世代1が0.521540から0.518528であった。
最良のエポック4の重みを採り、量子化誤差は平均0.522センチポーン、最大1.898センチポーン（合格条件2以下）、初期局面の評価は−8センチポーンである。
特徴の観測回数は、未観測が5,181個から4,539個へ減り、観測済みの中央値は219回から1,235回、p95は10,932回から163,477回へ増えたが、p05は8回にとどまり、偏りは残る。
線形モデルであるため損失の改善幅は小さく、世代1の検証損失0.52が世代0の0.42より高いのは、学習PST同士の対局が拮抗して結果を予測しにくいためと考えられる。
再学習した重みはコミットaf200b4（親はmaster、探索コードは直前のbfeb1aaと同一）で確定した。
固定ノードGSPRT（`match_runner --run-dir data/matches/pst-gen1-nodes100k --seed 20260828 --candidate commit:af200b4 --baseline commit:bfeb1aa --each nodes=100000 --concurrency 16 gsprt`）を同日13時30分に開始した。
固定ノードGSPRTは13時52分に判定`H1`で終了した。
結果は、ペア数224（有効223、手数上限による破棄1）、ペンタノミアル[6, 0, 58, 0, 159]、LLR 2.952、`engine_failures`はすべて0、経過1,319秒であった。
候補の得点率は84.3%（159ペアで2連勝、58ペアで1勝1敗、6ペアで2連敗、引き分けなし）で、終局はほぼすべて詰みである。
線形PSTの再学習で検証損失の改善が0.003にとどまるにもかかわらず固定ノードで大差がついた理由は、この測定だけでは切り分けられない。
候補の重みは基準の対局（世代1は基準と同じ学習PSTの自己対局）から学び、モデル出力Kが807から1,073へ変わったことで評価値の尺度が約33%大きくなり、delta pruningなど評価値の絶対量に依存する探索の枝刈りの効き方も変わっている。
設計判断第6のとおり固定ノードの結果は診断指標にとどめ、採否は時間制御GSPRTで決める。

## 目的

本マイルストーンは、評価関数マイルストーンが残した「教師不足が主因である」という判定を検証し、教師局面を1桁増やした条件で学習評価関数の強さがどこまで伸びるかを確定する。
測定の対象は、増量データで再学習した学習PSTと、同じデータで学習した残差線形項つきのP型NNUEの2つである。
採否の基準は前マイルストーンと同じ時間制御GSPRTであり、比較相手は採用済みの直前のコミットとする。
本マイルストーンは仮説の検証を目的とするので、どちらの評価関数も採用に至らなかった場合でも、測定結果と判定を記録すれば完了とする。

## 適用範囲

本マイルストーンで行う作業は次のとおりである。

- 学習器`train_pst.py`と復元する`train_nnue.py`を、バッチごとに特徴を作るストリーミング方式へ変更する。
- 自己対局生成器`selfplay_gen`に、対局中のランダム着手の注入を追加する。
- 現行の学習PSTで世代1データを1,000万局面生成する。
- 世代0と世代1のデータを合わせて学習PSTを再学習し、固定ノードと時間制御のGSPRTで採否を判定する。
- ブランチ`nnue-residual`のNNUEをmasterへ復元し、同じデータで学習して、bench NPS、固定ノードGSPRT、時間制御GSPRTを記録し採否を判定する。
- NNUEが時間制御で不採用となり、固定ノードで利得の証拠があり、プロファイルで推論が主要因と確認された場合に限り、推論の高速化を行って再判定する。
- 採用した評価関数についてHaChu比較の固定局数Eloを記録する。

次の事項は対象外とし、本マイルストーンの結果を見て別途起案する。

- 世代2以降の反復。1世代の生成と学習に要する時間が丸1日を超えるので、反復を続けるかどうかは世代1の結果で決める。
- 第1層の拡幅、出力バケット、相対王駒特徴（plans/evaluation.mdの次期候補の設計）。教師不足の解消後でなければ表現力の候補を判断できないという前マイルストーンの判断に従う。
- 手書きのモビリティ、王安全度、獅子関連項。minaseには利きマップがなく、`Position`への増分更新をゼロから実装する必要があるため、学習方式が本マイルストーンでも停滞した場合の比較対象として別途起案する。
- 静止探索の効率化。評価関数と独立に採否できるので別マイルストーンとする。

## 依存関係

前マイルストーンの成果物をそのまま使う。
データ形式`MNSD`と生成器`selfplay_gen`（コミット6bc33e1、80c430d）、学習PSTの特徴定義と重みファイル`MNPT`（コミット588363a、7e13888）、学習器`tools/train/train_pst.py`である。
NNUEの推論、差分更新、学習器はmasterから外されているので、コミット05362b1、3ee849c（masterに含まれる）とブランチ`nnue-residual`のコミット510fcde（残差線形項）から復元する。
`nnue-residual`はコミット3ee849cから分岐しており、その後のmasterのRust設計監査対応（探索スレッドの寿命と公開境界の変更）を含まない。
復元の契約はフェーズ4に記す。

測定環境は前マイルストーンと同じIntel Core Ultra 7 265KF（20コア、SMTなし）、メモリ30GB、NVIDIA GeForce RTX 3060 Ti 8GBである。
世代0のデータ`data/gen0.bin`と`data/gen0b.bin`（合計1,002,722局面、160,435,792バイト）は保持されている。
学習は`tools/train/.venv`のPython環境（PyTorch 2.13、CUDA有効）で行う。

## 設計判断

第1に、世代1データの目標を1,000万局面とする。
世代0の実データでは1局面あたり平均54.9個の特徴が発火しており、100万局面では13,680個の入力特徴は1特徴あたり平均約4,000回しか観測されなかった。
1,000万局面を加えると平均は約44,000回になる。
これは平均であって特徴ごとの観測回数は偏るので、学習時に特徴別の観測回数の分位点と未観測の特徴数を記録する。
世代0の生成速度約170局面/秒をそのまま使えば1,000万局面に約16時間を要するが、これはパイロット前の仮の見積りである。
ランダム着手の注入により記録の開始が遅れる対局が5局中4局あり、記録される局面は1局あたり平均で数十局面減るので、記録速度は世代0より下がる。
本生成の分割数と所要時間はフェーズ1のパイロットの実測で更新し、複数回に分けて実行して途中で失敗しても完了した分を残す。

第2に、世代1データは現行の学習PST（コミット7e13888の重み）で生成し、世代0データと合わせて学習する。
前マイルストーンの定義では、採用したネットで生成し直したデータからの学習が世代1である。
世代0データを捨てないのは、v0同士の対局で生じた局面が学習PST同士の対局には現れにくく、両方を混ぜたほうが局面の分布が広いためである。
世代0と世代1では探索値を出した評価関数が異なるので、探索値と勝敗の対応を表す勝率尺度Kも異なり得る。
教師値の勝率変換には世代別に推定したK（生成に使ったネットの検査和でファイルを分けて推定する）を使い、モデル出力のKは混合データで推定した1つの値とする。
2つのKを分ける学習器の変更は「学習器のストリーミング化」の節に記す。

第3に、分布収縮への対策は、対局中のランダム着手の注入と世代0データの併用とし、固定基準相手との対局データは採らない。
前マイルストーンはHaChuまたはv0を相手にした混在データを候補に挙げていた。
HaChuを相手にする対局は、HaChuが実装する規則（L1、L3、P0、P5、P6、R2、E1、E2）が学習に使う`engine-default`と異なり、先獅子の対象升を表す特徴の意味がL1の下で変わるため、教師局面として使えない。
v0を相手にする対局は、探索の評価関数を対局中に切り替える仕組みを必要とするが、利用者はv0を公開関数として残すだけで実行時の切替を設けないと決めている。
ランダム着手の注入は、Stockfishのデータ生成器が同じ目的で用いる方式であり、生成器への追加が1箇所で済む。

第4に、学習PSTの再学習を先に行い、NNUEはその結果を基準に測る。
再学習した学習PSTと現行の学習PSTの差には、局面数の増加だけでなく、生成に使った評価関数、ランダム着手、K、追加の更新ステップが含まれるので、この差は「データ増量を含む再学習工程の総合効果」である。
NNUEと再学習PSTの差は「同一データを用いた2つの学習方式の総合差」であり、モデルの構造だけでなく最適化の設定も含む。
それでも、この順序にしないと、NNUEが現行の学習PSTに勝った場合に、再学習工程の効果と学習方式の差のどちらが寄与したかを切り分ける手がかりがない。

第5に、NNUEは残差線形項つきの構成を起点にし、線形項を再学習した学習PSTで初期化する。
残差線形項は学習開始時の浮動小数点の出力を学習PSTに一致させる。
学習が進んだ後の評価が学習PSTより悪くならない保証はないので、初期状態（エポック0）を検証損失によるモデル選択の候補に含め、学習器が改善しなかった場合に学習PSTへ戻れるようにする。
量子化は第2層以降をi16（尺度4,096）とする前マイルストーンの判断材料第3に従い、ネット形式の版3をそのまま使う。

第6に、採否は時間制御GSPRTだけで決め、固定ノードGSPRTと速度の診断指標を併記して結果を解釈する。
利用者の懸念は、重い評価関数が探索速度を下げて総合では弱くなるという点にある。
固定ノードGSPRTは、速度差をおおむね抑えた固定ノード予算での棋力を測る。
ただし評価値は静止探索のstand-pat、delta pruning、置換表の値にも使われ、評価値の分布が変われば同じノード上限でも探索木が変わるので、固定ノードの結果を「知識だけ」の測定と見なすことはできない。
時間制御GSPRTは実戦条件での総合棋力を測り、これだけが採否を決める。
両者の差を速度の代価と解釈するには、bench NPSと、評価呼び出し1回と差分更新1回の時間の単体計測（前マイルストーンと同じ10万回の計測）を併記し、速度差がその候補で実際に生じていることを確認する。
時間制御対局での到達深さと静止探索ノードの比率は、`match_runner`が`bestmove`までの`info`行を捨て、探索結果にも静止探索のノード数がないため現行の構造では取れず、計測の追加は本マイルストーンの範囲外とする。
`H0`は「elo=5の改善があるとは言えない」という判定であり、利得がないことの証明ではない。
`pending`は判定保留であり、採否のどちらの根拠にもしない。
結果の組合せの読み方は次のとおりである。

- 時間制御で`H1`なら採用する。固定ノードの結果は記録にとどめる。
- 時間制御で`pending`なら候補は未判定のままとし、docs/sprt.md の契約どおり水準を変えて再測定するか変更の見直しへ戻る。高速化への移行や完了条件の充足には使わない。
- 時間制御で`H0`、固定ノードで`H1`なら、固定ノード予算では利得の証拠があるが実戦条件では確認できない。NNUEの場合は、診断指標で推論が速度低下の主要因と確認できたときに限りフェーズ5の高速化へ進む。
- 時間制御で`H0`、固定ノードで`H0`または`pending`なら、その候補は不採用とし、学習曲線と合わせて教師不足の判定を再検討する材料として記録する。

速度の代価の粗い事前見積りとして、HaChu戦の要因分析（持ち時間4倍で約265 Elo回復）から持ち時間2倍あたり約125 Eloが得られる。
ただし4倍測定は24ペアで95%信頼区間が[−71, +102]と広く、線形性も確かめられていない。
この値を採否に使わず、自己対局での感度を参考測定として別に記録する（採否測定の節）。

第7に、推論の高速化は条件付きのフェーズとし、設計判断第6の第2の組合せに該当し、かつプロファイルで推論が主要因と確認された場合に限って行う。
前マイルストーンの単体計測では、探索1ノードあたり約0.77マイクロ秒に対して第2層以降の推論が0.68マイクロ秒を占め、差分更新は0.068マイクロ秒である。
測定機のCPUはAVX2を持ちAVX-512を持たない。
一方で`Cargo.toml`は`unsafe_code = "forbid"`であり、`std::arch`のintrinsicはこの方針と両立しないので、安全なSIMDクレート、自動ベクトル化の誘導、方針の変更のいずれを採るかは発動時に設計する。
発動しない場合に実装の詳細を先に決めないのは、YAGNIの原則による。

第8に、本マイルストーンは世代1の1回で区切り、生成と学習の反復は行わない。
1世代の生成に約16時間、学習と測定に丸1日を要するので、反復を続けるかどうかは世代1の結果を見てから判断するほうが手戻りが小さい。

第9に、教師値の定義、除外規則、教師探索のノード数、規則セットは前マイルストーンから変えない。
教師値は探索値と対局結果の混合でλ=0.75、教師探索は100,000ノード、規則セットは`engine-default`、除外は詰み帯、捕獲または成りの最善手、同一対局内の再出現である。
変数を増やすと、結果の差がデータ量によるものかどうかを判別できなくなる。

## ランダム着手の注入

生成器`selfplay_gen`の`generate`に、1局あたりのランダム着手の上限回数を指定する必須引数`--random-moves`を追加する。
値は0から80までとし、0を指定すると注入しない。
対局ごとに、序盤ランダム化とは独立に派生させたシードから注入回数を0から上限までの一様乱数で決め、注入する手数を序盤ランダム化の終了手数を`p`として`[p, p+80)`の範囲で重複なく一様に選ぶ。
注入する手番では合法手から一様に選んだ着手を指し、教師探索は行わない。
早期終局や手数上限で予定した注入を消化できない場合は、その局の実施回数が予定回数を下回る。

局面の記録は、最後に予定した注入手数より後の局面から始める。
教師値の25%は対局結果であり、注入前の局面の探索値は最善手の手順を前提とするのに対し、対局結果はランダム着手を含む別の手順から生じるので、両者を混ぜると教師値が歪む。
記録を最後の注入より後に限れば、記録されたすべての局面は、以後を生成器の方策だけで指した対局の結果を持つ。
注入回数が0の対局（上限4なら5局に1局）は序盤から記録されるので、序盤の局面はこれらの対局から得る。
注入後の局面には、除外規則をそのまま適用する。

実装上の制約は次のとおりである。
注入の判定は教師探索の前に置き、探索の後で着手だけを差し替えてはならない。
記録の境界には最後に実施した注入ではなく最後に予定した注入の手数を使い、注入回数が0の対局では境界を序盤ランダム化の終了手数`p`に置いて現行どおり`p`の局面から記録する。
注入の乱数は対局番号から派生させた個別のシードだけから取り、ワーカー単位の乱数状態を使わない。
これにより、現行の対局番号順の書き出しと合わせて、並列度に依存しない決定性を維持する。
境界より前の手番で着手選択のために行う教師探索は、探索回数の集計に含めるが記録対象にはならないので、生成のサマリでは探索回数、記録対象の局面数、記録対象のうち除外規則で除いた数（理由別）、記録数を分けて出力し、除外率は記録対象の局面数を分母とする。

上限回数は世代1の生成で4とし、これは仮の設定である。
Stockfishの生成器は序盤24手以内に最大5回のランダム着手を入れる。
中将棋の1局は平均約290手と長く、序盤ランダム化が8〜16手あるので、その直後の80手に散らせば、序盤から中盤への移行部の分布が広がる。
生成のサマリには、予定回数と実施回数の合計、注入した手数の相対値（`p`からの差）のヒストグラムを出力する。
パイロット測定は、同じコミットとシードで上限0の対照パイロットと上限4のパイロットを128局ずつ行い、除外率、終局の内訳、探索値の分布（平均、標準偏差、分位点）、対局をまたいだ局面の重複率（記録局面の探索キーで数える）の4項目を比べる。
世代0のパイロットには探索値の標準偏差と重複率の記録がないため、比較の基準は対照パイロットとする。
合否の基準は、理由別の除外率と詰みによる終局の割合が対照との絶対差5ポイント以内、探索値の標準偏差が対照との相対差20%以内、重複率が対照以下、の3つとする。
満たさない場合は上限回数を2に下げて再測定し、それでも満たさなければ注入を見送り世代0の併用だけで進める。

## 学習器のストリーミング化

現行の`train_pst.py`と`nnue-residual`の`train_nnue.py`は、全局面の特徴番号（1局面あたり最大145個のint32）を通常と鏡映の2本、NNUEでは2視点で4本作り、すべてをGPUへ置く。
1,100万局面では1本が約6GiBになり、NNUEの4本で約24GiBとなってGPUの8GBに収まらず、CPU側の一時配列を含めるとメモリ30GBも超える。
そこで両学習器を、複数ファイルをメモリマップのまま保持し、バッチごとに盤面を読んで通常と鏡映の特徴をCPUで生成しGPUへ転送する方式へ変更する。
これは前マイルストーンの設計書が学習器の実装方針として定めた契約（メモリマップと疎な加算、密なテンソルを作らない）に沿う。
世代1の生成前に、世代0の100万局面で1エポックの最大常駐メモリ、VRAM、局面/秒を測り、1,100万局面での学習時間を見積もる。

教師値の勝率変換に使うKとモデル出力のKも分ける。
現行の両学習器は1つのKを探索値の変換とモデル出力の両方に使う。
変更後は、入力ファイルをヘッダのネット検査和で世代に分け、世代ごとに教師Kを`estimate-k`と同じ推定で求めて教師値の変換に使い、`--k`で与えるモデル出力のKは混合データで推定した値とする。
世代別の教師Kは学習ログに記録する。

検証分割は、対局の来歴に基づく安定した対局IDで行う。
対局IDはファイルのヘッダのシードと対局番号の組とし、そのハッシュを20で割った余りが0の対局を検証に充てる。
現行の読み込み器は後続ファイルの対局番号に前ファイルの最大番号を加えてから分割するため、分割がファイルの順序に依存する。
シードが重複するファイルの組は読み込み時に拒否し、同じファイルの二重指定も拒否する。
検証損失は全体と世代別を併記する。

## 学習の構成

学習PSTの再学習は、世代0と世代1のファイルをすべて渡し、現行の重み（`nets/pst.bin`）で初期化し、学習率3で行う。
学習率は前マイルストーンで1回だけ粗く探索して固定したものであり、本マイルストーンでは再探索しない。
エポック数は10とし、エポック0（初期状態）を含めて検証損失が最良のエポックの重みを採る。
現行の`train_pst.py`は最終エポックを保存するので、NNUEと同じ最良エポックの規則に揃える。

NNUEの学習は、復元した`train_nnue.py`で、残差線形項を再学習した学習PSTで初期化し、NNUE側の出力層を0で初期化して行う。
バッチ4,096、学習率0.01、余弦減衰、重み減衰0、シード1、CUDAは前マイルストーンの最終設定を引き継ぐ。
前マイルストーンの60エポックは約14,000更新であり、1,100万局面の20エポックは約51,000更新になる。
余弦減衰の時間軸はこの20エポックに合わせ、エポック0を含めて検証損失が最良のエポックのネットを採る。
世代0では残差線形版の検証損失が2エポック目で最良となり以後は過学習したので、世代1で検証損失が同じデータで再学習した学習PSTの水準を下回り続けるかどうかを記録する。
検証損失の改善は、前マイルストーンで損失が近くても対局で大敗した例があるとおり、棋力の証拠にはならない。
教師不足の判定は、学習曲線と固定ノードGSPRTの両方が整合する場合に限って検証されたと見なす。
量子化誤差の合格条件は前マイルストーンと同じK/40以下とする。

## 採否測定

測定は docs/sprt.md の標準手順に従い、`Threads=1`、同時対局数16、シードを明示して行う。
固定ノードGSPRTは`--each nodes=100000`、時間制御GSPRTは`--each time=30000+300,byoyomi=500`である。
各候補について、bench NPS（深さ5、3回の中央値）と、評価呼び出し1回と差分更新1回の時間の単体計測を診断指標として記録する。

測定用のコミットは、採用済みの基準を直接の親に置く。
学習PSTの再学習は、現行の学習PST（master）を親にコミットし、固定ノードGSPRTと時間制御GSPRTを行い、時間制御で`H1`ならmasterへ取り込む。
NNUEは、再学習した学習PSTが採用されればそのコミット、されなければ現行の学習PSTのコミットを親に置き、ネットを埋め込んだコミットについてbench NPS、固定ノードGSPRT、時間制御GSPRTを行う。
再学習PSTが不採用の場合、その重みはNNUEの線形項の初期値としてだけ使い、NNUEのコミットには不採用のPSTコミットを含めない。
フェーズ5へ進んだ場合、高速化を含むNNUE全体を採用済みの基準と時間制御GSPRTで測るため、NNUEと高速化を1つの候補コミットにまとめる。
高速化単独の効果はbench NPSの差で記録する。

参考測定として、自己対局での持ち時間の感度を1回だけ測る。
現行のコミット同士で、候補側に`time=60000+600,byoyomi=1000`、基準側に`time=30000+300,byoyomi=500`を与えた固定200ペアのEloと95%信頼区間、`time_forfeits`を記録する。
この値は設計判断第6の事前見積りを自己対局の条件で置き換えるためのものであり、採否には使わず、線形性も仮定しない。
時間制御対局は実時間に依存するので、学習や生成と並行させず単独で実施する。

採用した評価関数については、docs/sprt.md の外部エンジン比較の手順でHaChuとの固定局数Elo（200ペア）を記録する。
この結果は採否の条件ではないが、採用した評価関数がある場合、その実施と記録は完了条件に含める。

## 実装フェーズ

### フェーズ1　学習器のストリーミング化とランダム着手の注入

`train_pst.py`をストリーミング方式、安定した検証分割、世代別の教師Kへ変更し、世代0データで学習した結果が現行の`nets/pst.bin`と量子化誤差の範囲で一致することを確認する。
世代0の100万局面で1エポックの最大常駐メモリ、VRAM、局面/秒を測る。
`selfplay_gen`にランダム着手の注入とサマリ出力を実装する。
上限0の対照パイロットと上限4のパイロットを128局ずつ行い、生成速度と「ランダム着手の注入」の節の合否基準を記録し、本生成の分割数と所要時間を更新する。
実装はcodexへ委任し、レビュー後にコミットする。

### フェーズ2　世代1データの生成

フェーズ1のコミットで、シードを変えた5回の実行により約1,000万局面を生成する。
各実行のコマンド全文、サマリ（局数、記録局面数、除外率、注入の集計、生成速度、経過時間）、生成コミットを記録する。
`inspect`で全ファイルの形式を検査する。

### フェーズ3　学習PSTの再学習

世代別の教師Kと混合のモデル出力Kを推定して記録し、学習PSTを再学習してコミットし、固定ノードと時間制御のGSPRTで採否を判定する。
学習曲線（訓練損失と検証損失、世代別の検証損失）、特徴別の観測回数の分位点と未観測数、量子化誤差、bench NPS、診断指標を記録する。

### フェーズ4　NNUEの復元と世代1の学習

NNUEの推論、差分更新、学習器をmasterへ復元する。
復元の契約は次のとおりである。

- ブランチ`nnue-residual`の先端から`src/eval/nnue.rs`、`tools/train/nnue_net.py`、`tools/train/train_nnue.py`、`nets/nnue.bin`を丸ごと復元する。
- 現行の`SearchSnapshot`、検証済み`SearchLimits`、`Result`を返す同期探索、`SearchHandle`の`Drop`とパニック時のワーカー回収の契約を維持する。
- 同期探索へは`&Network`、非同期探索へは`Arc<Network>`を明示的に渡す。
- `Undo`はcrate内だけで使い、crate rootから再公開しない。両視点の特徴関数は`pub(super)`で再導入し、旧公開境界を戻さない。
- USI、CECP、bench、match_runner、selfplay_genの評価器の取得と検査和の記録を`Network`へ移す。
- 設計監査後の`PieceCode::new`が`Option`を返す変更にNNUEのテストを適合させる。

`train_nnue.py`をストリーミング方式と世代別の教師Kへ変更し、`cargo test`の全通過を確認する。
復元と学習器の変更はcodexへ委任する。
混合データで残差線形版を学習し、学習曲線と量子化誤差を記録し、ネットを埋め込んだコミットについてbench NPS、固定ノードGSPRT、時間制御GSPRT、診断指標を記録する。
持ち時間の感度の参考測定は、学習と生成のどちらとも重ならない時間帯に単独で行う。

### フェーズ5　推論の高速化（条件付き）

設計判断第7の条件に該当した場合に限り、高速化の方式を設計して実装し、bench NPSと時間制御GSPRTを再度行う。
差分更新と全再計算の一致テスト、およびスカラー実装との出力一致テストで正しさを固定する。

### フェーズ6　記録

採用した評価関数のHaChu比較の固定局数Eloを記録し、本書の実施状況とROADMAP.mdの状態表と現在地を更新する。
教師不足の判定の検証結果と、次期候補（世代2、表現力の候補、手書き項）の要否を起案材料として残す。

## 検証

- `cargo test`で、ランダム着手の注入が同じシードと引数で同じ出力を生むこと、注入回数と手数が指定の範囲に収まること、記録が最後の注入より後の局面から始まることが固定されている。
- 注入を0にしたフェーズ1のコミットの出力が、注入前のコミットの出力とヘッダの生成コミット欄を除いて一致することを、同じシードと引数の生成で手動確認する。
- ストリーミング化した`train_pst.py`が世代0データで現行の`nets/pst.bin`と量子化誤差の範囲で一致する重みを出す。
- 両学習器が、世代別の教師Kを検査和でファイルを分けて推定し、モデル出力のKと分けて使うことをテストで固定している。
- 復元したNNUEについて、差分更新と全再計算の一致、Python参照実装との照合、ネットファイルの検証が前マイルストーンと同じテストで通る。
- 各測定の`engine_failures`と`time_forfeits`が0である。
- 生成した全ファイルが`inspect`の検査を通る。

## 完了条件

- 世代1データが1,000万局面以上生成され、生成条件とサマリが本書に記録されている。
- 再学習した学習PSTとNNUEのそれぞれについて、学習曲線、量子化誤差、bench NPS、診断指標、固定ノードGSPRT、時間制御GSPRTが本書に記録され、設計判断第6に従う判定が下されている。
- 採用した評価関数がある場合、そのコミットが時間制御GSPRTで採用済みの直前のコミットに対し`decision: H1`であり、HaChu比較の固定局数Eloが記録されている。
- 教師不足の判定の検証結果と次期候補の要否が本書に記録されている。
- ROADMAP.mdの状態表と現在地が更新されている。

## 参考資料

- plans/evaluation.md：前マイルストーンの設計、世代0の測定、次期候補の設計。
- plans/match-harness.md：HaChu戦の要因分析（探索実測と持ち時間4倍のElo）。
- docs/sprt.md：測定の標準手順。
- Stockfish旧`gensfen`のランダム着手オプション（`random_move_count`、`random_move_minply`、`random_move_maxply`）と、保存した最善手と実際のランダム着手の不一致を報告したissue #3762。

# Lazy SMPの設計書

## 実施状況

2026年8月13日に本設計書を起案した。

2026年8月22日に着手した。
着手条件である探索部マイルストーンの完了（同日）と外部対局接続のCECP対局進行（2026年8月14日）は両方満たされていた。
実装はcodexへ委任し、Claudeが設計整合のレビュー、修正、検証、およびコミットを行った。
実装前のbench署名は、depth 3で総ノード数1,190,540、depth 4で4,573,594である。

同日にフェーズ1（原子型置換表、コミット49f6b8f）を完了した。
エントリは`critical`（検証キー32ビット、評価値16ビット、深さ8ビット、バウンド2ビット、予約6ビット）と`advisory`（指し手25ビット、世代8ビット、予約31ビット）の2個の`AtomicU64`からなる16バイトであり、`probe`・`store`・`new_search`は`&self`で動作する。
公開順序は設計どおり`advisory`をRelaxedで書いた後に`critical`をReleaseで公開し、読み取りは`critical`をAcquireで読んでから`advisory`をRelaxedで読む。
予約ビット、バウンド、検証キー、指し手の復号に失敗したエントリは未命中として扱い、指し手の復号は失敗可能な`Option<Move>`へ変更した。
64ビット原子型のない対象は`compile_error!`で拒否する。
テストはD7-TT-08〜10（16バイト配置と詰め込みの往復、破損エントリの未命中化、4スレッドによる同一スロット集合への並行probe/store）を追加して411件全緑、clippy警告なし、fmt通過であり、bench署名は実装前と完全一致した。

同日にフェーズ2（探索チーム）とフェーズ3（共通停止と予算）を1コミット（62a5a58）で完了した。
両フェーズは共有状態の定義を介して不可分であったため、設計書の区分は保ちつつ実装とコミットは一体で行った。
`start_search`と`search`にワーカー数`NonZeroUsize`を明示引数として追加し、`DEFAULT_THREADS`（1）を定数化した。
`start_search`が生成するスレッドは調整役兼主ワーカーとなり、`thread::scope`内で補助ワーカーを生成して全員をjoinする。同期版`search`も同じ調整役経路を通り、単一ワーカー専用の旧経路は残していない。
補助ワーカーはルート合法手列をワーカー番号だけ左回転してから既存の安定な順序付けを適用し、結果と進捗は主ワーカーだけが返す。
共有状態は外部停止フラグ、チーム停止フラグ、`fetch_max`で優先順位を保つ停止理由、総ノード数、開始時刻、hard limitであり、ノード上限は`compare_exchange`による1ノード単位の予約で探索チーム全体へ適用される。
外部停止とチーム停止の確認は毎ノードの原子読み取りとし、hard limitの時計確認だけを従来どおり4096局所ノードごとに維持した。
Claudeのレビューで、scope終了後に外部停止を再確認して停止理由を上書きする行を削除した。自然完了した探索の理由まで`ExternalStop`へ書き換えるためである。
テストはD7-SMP-01〜07（全停止経路と1回だけの完了通知、チーム全体のノード上限、主ワーカーだけの単調な進捗、単一ワーカーの完全再現、停止理由の優先順位、ルート回転後の安定な優先順位、世代の1回更新）を追加して416件全緑、clippy警告なし、fmt通過であり、bench署名は実装前と完全一致した。
ThreadSanitizerによる競合試験は、本環境にRust nightlyが導入されていないため実施していない。

同日にフェーズ4（プロトコルとbench、コミット90c1dd1）を完了した。
USIは`option name Threads type spin default 1 min 1 max 256`を宣言し、`setoption name Threads`で1以上256以下の整数だけを受理する。CECPは`feature smp=1`を宣言し、`cores N`を同じ範囲で受理する。
いずれも不正値には固定エラー（USIは`info string error: Threads must be an integer from 1 to 256`、CECPは`Error (invalid command): cores`）を返して直前の設定を保持し、探索中に届いた変更は既存の待機列を経て次の探索から適用される。
benchは`--threads`と`--repetitions`を受け取り、反復時はウォームアップ1回の後に各回の到達深さ、総ノード数、経過時間、ノード毎秒と、それらの中央値を出力する。
`docs/sprt.md`は完全再現契約を`Threads=1`へ限定し、並列測定の記録項目と同時対局数の上限を追記した。
テストはD6-USI-35〜36とD6-CECP-33〜34などを追加して423件全緑、clippy警告なし、fmt通過であり、既定出力のbench署名は一致した。
USI実機では`Threads=4`の`go depth 4`が深さ1から4の`info`を単調に出力し、`bestmove`を1回だけ返した。

フェーズ5の固定局面benchは、Intel Core Ultra 7 265KF（20物理コア、SMTなし）、depth 4、ウォームアップ1回を除く5回の中央値で次のとおりであった。

| Threads | 総ノード数 | 経過時間 | ノード毎秒 | 速度比 |
|---|---|---|---|---|
| 1 | 4,573,594 | 4.513秒 | 1,013,527 | 1.00 |
| 2 | 8,727,316 | 4.427秒 | 1,971,148 | 1.94 |
| 4 | 17,145,840 | 4.587秒 | 3,745,109 | 3.70 |

ノード毎秒はワーカー数にほぼ比例して伸び、毎ノードの原子加算と共有置換表による競合は2ワーカーで約3%、4ワーカーで約8%の損失に留まる。
一方、固定深さの経過時間はワーカー数によらずほぼ同じであり、補助ワーカーが温めた置換表が主ワーカーの同一深さの完了を早める効果は本水準では観測されなかった。
固定深さは採否条件ではないため、棋力への効果は時間制御GSPRTで判定する。

フェーズ5の時間制御GSPRTは、測定用ブランチ`lazy-smp-threads2`のコミット5f1f74b（コミット90c1dd1から`DEFAULT_THREADS`だけを2へ変更）を候補、90c1dd1を基準として、同日に開始した。
コマンドは`match_runner --seed 20260822 --candidate commit:5f1f74b --baseline commit:90c1dd1 --each time=10000+100,byoyomi=200 --concurrency 8 gsprt`であり、環境はIntel Core Ultra 7 265KF（物理20コア、論理20、SMTなし）、`USI_Hash`は既定の256MB、同時対局数8（8×2=16コアを使用）、候補Threads=2、基準Threads=1である。
開始から約55分の時点で有効81ペア、ペンタノミアル[14, 1, 51, 0, 15]、LLR −0.003、判定保留、time_forfeitsは0であった。
ペア得点の平均0.503はElo換算で約+2であり、95%信頼区間はおよそ±45である。
真のElo差が0付近であれば判定までの期待ペア数は約13,000（約1.5ペア/分で150時間超）になるため、継続の可否を利用者へ諮った。

同日、利用者は「設計を改訂して再測定する」を選択し、測定を約60分で停止した。
停止時点の最終状態は有効92ペア、ペンタノミアル[17, 1, 55, 0, 19]、LLR 0.031、判定保留、engine_failuresは全0（time_forfeits 0）であり、この測定は判定保留の記録として残す。
原因は次のとおり分析した。
固定深さbenchで総ノード数が2倍になる一方で経過時間が不変であることは、主ワーカーが補助ワーカーの置換表エントリからほぼ利益を得ていないことを意味する。
第1版の多様化はルート合法手列の回転だけであったが、指し手順序付けは置換表手を先頭へ入れ替えるため、根の置換表エントリが書かれた後は補助ワーカーも主ワーカーと同じ手から同じ深さを探索し、同順位の非捕獲手の順序だけが異なる。
その結果、補助ワーカーは主ワーカーの複製に近い探索を同じ深さ列で行い、同じ局面の同じ深さのエントリを重複して書くため、主ワーカーの反復を早める新しい情報をほとんど供給しない。
また、1手あたり約0.4秒・到達深さ4〜5の浅い探索では、補助ワーカーが主ワーカーより先に深い情報を用意する余地がそもそも小さい。
改訂候補として、補助ワーカーkの反復を深さ1+kから始める深さオフセット方式を起案し、codexの読み取り専用レビューを受けた。
レビューは、開始深さをずらしても補助ワーカーが時間的に先行する保証はないこと（深さ1の所要時間は無視できるため両者はほぼ同時に同じ深さへ入る）、同一キーの置換表エントリは既存が深くても上書きされるため補助ワーカーの深い値が消され得ること、ワーカー番号をそのままオフセットにするとThreads=256で深さ256へ直行すること、benchゲートの合格条件が未定義であることを指摘した。
これを受けて、製品コードを変えずに作業ツリー外の実験ツリーで次の診断を行った（Intel Core Ultra 7 265KF、bench 15局面、反復の中央値または単回）。

| 方式 | 深さ | Threads=1 | Threads=2 | 備考 |
|---|---|---|---|---|
| 現行（同一深さ列＋ルート回転） | 5 | 11.09秒 | ― | 主ワーカー12.76Mノード、negamax照会242K、ヒット96K、カットオフ79K |
| 現行 | 6 | 23.76秒 | 23.43秒（−1.4%） | 主ワーカーのノード+1.2%、カットオフ−1.7% |
| 深さオフセット（補助は深さ2から） | 5 | ― | 13.08秒（+18%） | 主ワーカー13.78Mノード、カットオフ76.8K |
| 深さオフセット＋同一キーは深い方を保持 | 5 | 11.14秒（ノード差2） | 12.60秒（+13%） | 同一キー規則の単一ワーカーへの影響は2ノード |
| 同上 | 6 | ― | 29.68秒（+25%） | |
| 深さスキップ（補助は1, 3, 5、上限で止める） | 5 | ― | 9.09秒（−18%） | 主ワーカー9.99Mノード（−22%） |
| 同上 | 6 | ― | 21.92秒（−7.7%） | 上限6で補助が5の後に6を並走 |
| ストライド（補助は1, 3, 5, 7…、上限なし） | 5 | ― | 9.18〜9.35秒（−16〜17%） | 同一キー規則の有無で差は小 |
| 同上 | 6 | 23.83秒 | 25.1〜25.5秒（+5〜7%） | 主ワーカーのノード+1.2%、カットオフ−1.7%。補助側はヒット458K・カットオフ381K |

診断から次の3点が確定した。
第1に、単一ワーカーの深さ5探索では、置換表を照会する残り深さ1以上のnegamaxノードが全ノードの約2%（242K／12.76M）であり、残りは置換表を使わない深さ0ノードと静止探索である。置換表共有が減らせるのはこの2%のノードを根とする部分木だけであり、補助ワーカーが主ワーカーのカットオフ数を増やさない限り利益は出ない。
第2に、利益が出るのは「補助ワーカーが主ワーカーと同じ深さを、時間的に先行して探索している」構成だけである。深さスキップ方式の深さ5（補助が深さ5を早く開始）では主ワーカーのノードが22%減ったが、補助が1つ深い反復にいる構成（深さオフセット、ストライドの偶数深さ）では補助は木の探索位置で主ワーカーより後ろにあり、主ワーカーの利益にならずにメモリと置換表の競合（ワーカーあたりNPS約7〜12%低下）だけが残って経過時間が悪化した。
第3に、現行方式の補助ワーカーは主ワーカーと同じ深さを遅れて探索する複製であるため、主ワーカーに利益を与えない（逆に補助側は主ワーカーの書き込みから大きく利益を得るが、その結果は捨てられる）。
主ワーカーだけが結果を返す設計では、補助ワーカーが先行する時間帯の利益しか回収できないため、反復深化が進むたびに先行と後行が入れ替わるスキップ系の方式は実戦で利益と損失が交互に現れ、純利益は小さいと見込まれる。
主ワーカーの静止探索コストが支配的である限り、置換表共有だけのLazy SMPで`Threads=2`が有意な棋力差を示す見込みは低く、静止探索での置換表利用や静止探索の効率化といった単一ワーカーの探索改良が先決であると判断した。
実験ツリーは破棄し、製品コードは現行方式（コミット90c1dd1）のまま変更していない。

同日、利用者は「採用保留で一旦閉じる」を選択し、本マイルストーンを実装完了・採用保留として閉じた。
完了基準のうち、`Threads=1`の不変性、`Threads=2`以上の停止・予算・1回だけの`bestmove`、USIの`Threads`とCECPの`cores`の3点は満たしており、時間制御GSPRTでのH1到達だけが未達である。
原子型置換表、探索チーム、共通停止と予算、プロトコル設定、benchの並列測定は製品に残し、既定ワーカー数は1を維持する。
次は、静止探索での置換表利用や静止探索の効率化といった単一ワーカーの探索改良を別マイルストーンとして起案し、その完了後に測定用ブランチ`lazy-smp-threads2`と同じ手順（`DEFAULT_THREADS`だけを2へ変えた候補コミット対基準コミットの時間制御GSPRT）で`Threads=2`対1を再測定する予定であった。

同日、利用者は診断で特定した原因を本マイルストーン内で修正するよう指示し、マイルストーンを再開した。
改訂は第2版として本書の設計判断、探索チーム、共有置換表、停止と探索予算、採否測定の各節へ反映し、実装フェーズ6〜8を追加した。
改訂の骨子は、静止探索での置換表の照会と記録（原因1への対処）、同一キーの深さを考慮した置換規則、停止時に最も深い反復を完了したワーカーの結果を採用する方式と補助ワーカーの深さスキップ（原因2と3への対処）、総ノード数カウンタのキャッシュライン分離と一括予約（NPS低下への対処）、および採否測定の時間制御を並列探索の用途に合う水準へ事前に固定することである。
単一ワーカーの探索を変える改訂（静止探索の置換表と置換規則）は`Threads=1`のGSPRTで先に採否を判定し、並列の改訂はその採用後に固定深さbenchのゲートを経て時間制御GSPRTで判定する。

起案時点では、探索部マイルストーンのフェーズ1、2、3、5と、外部対局接続マイルストーンのフェーズ1が完了している。
本マイルストーンは、探索部の静止探索と第2層の探索改良の採否が完了し、外部対局接続のCECP対局進行が完成した後に着手する。
この順序により、単一ワーカー探索の基準を固定してから並列化し、探索改良と並列化の効果を別々に測定する。

起案にあたり、2026年8月13日時点のStockfishとやねうら王の実装を一次資料として確認した。
両実装とも、複数の探索ワーカーが独立に反復深化を実行し、置換表を共有した後に探索チームの結果を集約する構造を採っている。
ただし、両実装が許容する競合をRustの通常メモリアクセスで再現すると未定義動作になるため、Minaseではデータ競合のない原子型の置換表を使う。

## 目的

本マイルストーンは、対称型マルチプロセッシング（symmetric multiprocessing、SMP）環境で複数の探索ワーカーを同時に動かし、同じ壁時計時間で到達できる探索深さと棋力を向上させる。
方式は、各ワーカーが独立した反復深化アルファベータ探索を行い、置換表だけを共有するLazy SMPとする。

Lazy SMPは着手を乱数で変える機能ではない。
ワーカー数が2以上の場合は共有置換表へのアクセス順が実行時スケジューリングに依存するため、同じ局面と制限でも結果が変わり得るが、この非決定性を着手多様化の手段として扱わない。

完了基準は次の4点とする。

- `Threads=1`で、着手、評価値、深さ、ノード数、主変化、および固定深さbenchの総ノード数が実装前と一致する。
- `Threads=2`以上で、全ワーカーが同じ停止要求と探索予算に従い、合法な最善手を1回だけ返して終了する。
- 時間制御のコミット対コミット一般化逐次確率比検定（GSPRT）で、`Threads=2`が`Threads=1`に対してH1へ到達する。
- Universal Shogi Interface（USI）の`Threads`とChess Engine Communication Protocol（CECP）の`cores`から、次の探索に使うワーカー数を設定できる。

## 適用範囲

本マイルストーンでは、次の作業を行う。

- 置換表を、複数ワーカーが共有できる原子型の16バイトエントリへ変更する。
- 現行の探索スレッドを探索チームの調整役兼主ワーカーへ変更し、探索ごとに補助ワーカーを生成して終了時に全員を待機する。
- 停止要求、hard limit、soft limit、およびノード上限を探索チーム全体へ適用する。
- USIの`Threads`オプションとCECPの`smp=1`および`cores`コマンドを実装する。
- 固定局面benchへワーカー数と反復回数を追加し、単一ワーカーの回帰と並列時の速度向上を分けて測定する。
- 並列探索時の再現性と測定条件を`docs/sprt.md`へ追記する。

次の項目は対象外とする。

- 先読み（ponder）、分散メモリ探索、非均一メモリアクセス（non-uniform memory access、NUMA）配置、中央処理装置（CPU）の固定割り当て、および永続ワーカープールは実装しない。
- 補助ワーカーの結果による投票と複数主変化出力は実装しない（最深ワーカーの採用は第2版で採用した）。
- 乱数によるルート手選択、低棋力化、および定跡手の乱択は実装しない。
- 置換表のクラスタ化と停止確認周期の短縮は、実測で必要性が示されるまで実装しない（ノード数の一括予約は第2版で採用した）。
- `match_runner`へ候補側と基準側の機能を切り替えるオプションは追加しない。
- `src/core/`と評価関数は変更しない。静止探索の置換表利用は探索部の変更だが、本マイルストーンの原因修正として第2版の適用範囲に含める。

## 依存関係

探索部の静止探索と第2層の探索改良を先に完了させる。
探索順序、枝刈り、および延長を並列化と同時に変更すると、棋力差と性能差の原因を分離できないためである。

外部対局接続のCECP対局進行も、本マイルストーンのプロトコル接続フェーズより先に完了させる。
CECPの状態機械と探索中コマンドの所有権は`engine-connectivity.md`にあり、本マイルストーンは完成した境界へ`smp`と`cores`だけを追加する。

評価関数の本格化は本マイルストーンの着手条件ではない。
Lazy SMPの採否は、着手時点で完成している単一ワーカー探索を基準として独立に測定する。

## 設計判断

| 項目 | 決定 |
|---|---|
| ワーカー数 | `Threads`は主ワーカーを含む探索ワーカーの総数とし、既定値1、最小値1、最大値256とする。入力値をCPU数に合わせて暗黙に変更しない。 |
| 探索制限との境界 | ワーカー数は停止条件ではないため`SearchLimits`へ追加せず、`NonZeroUsize`として探索開始関数へ明示的に渡す。 |
| ワーカーの寿命 | 現行どおり探索ごとにスレッドを生成する。永続ワーカープールは導入しない。 |
| 共有状態 | 置換表、停止状態、停止理由、総ノード数、開始時刻、および時間予算だけを共有する。総ノード数は停止フラグとは別のキャッシュラインに置く（第2版）。局面、合法手生成器、探索経路、主変化、および将来の探索履歴はワーカーごとに所有する。 |
| 最善手 | 停止時に最も深い反復深化を完了したワーカーの結果を採用し、同じ深さなら主ワーカーを優先する。補助ワーカーは完了した反復の結果を保持して調整役へ返すが、進捗は送らない（第2版）。 |
| 多様化 | 補助ワーカーは一部の深さを飛ばして主ワーカーより先の反復へ進む。補助ワーカーkは深さdのうち`d == 1`または`(d - 1) % (k + 1) == 0`を満たす反復だけを探索する（k=1なら1、3、5、…）。ルート合法手列の回転、時刻、および乱数は使わない（第2版。初版のルート回転は置換表手の先頭入れ替えで打ち消されることが判明した）。 |
| 置換表容量 | `USI_Hash`とCECPの`memory`は全ワーカーで共有する置換表の総容量を表し、ワーカー数倍に増やさない。 |
| ノード上限 | `go nodes N`は探索チーム全体で最大Nノードとし、ワーカーごとにNノードを与えない。各ワーカーは局所カウンタで数え、1024ノード単位で共有カウンタへ加算または予約する。`Finished.nodes`がNを超えない契約は維持する（第2版）。 |
| 進捗 | `Progress`は主ワーカーのイテレーション完了時だけ送る。`nodes`は送信時点のチーム総数とし、`Finished`は全ワーカーの終了後に1回だけ送る。最深ワーカーの採用により、`Finished.depth`は最後の`Progress.depth`以上になる（第2版）。 |
| 再現性 | `Threads=1`は現行の決定性を維持する。`Threads>1`は固定深さと固定ノード数でも完全再現を保証しない。 |
| 対応環境 | 原子型の64ビット整数を利用できる共有メモリ環境を対象とする。該当しないコンパイル対象はコンパイル時に明示的に拒否する。 |

## 探索チーム

`start_search`が生成する現行の探索スレッドを、探索チームの調整役兼主ワーカーとする。
このスレッドは共通状態を構築し、`std::thread::scope`内で`Threads-1`本の補助ワーカーを生成した後、自身で主ワーカーの探索を実行する。
`scope`を抜ける前に全補助ワーカーを待つため、正常終了、停止、および`panic`のいずれでも孤児スレッドを残さない。

`SearchHandle`の公開契約は探索スレッド1本から探索チーム全体へ読み替える。
`request_stop`はチーム共通の停止状態を更新し、`join`は全ワーカーの終了後に共有置換表を返す。
ワーカー生成に失敗した場合は少ないワーカー数へ縮退せず、探索チームを失敗させて既存のチャネル切断とjoinエラーの経路へ接続する。

`SearchSnapshot`は調整役が所有し、scope内の全ワーカーが不変参照する。
各ワーカーは根局面を複製し、`Searcher`、合法手生成器、探索経路、主変化、および局所ノード数を個別に持つ。
置換表の世代は調整役がワーカー生成前に1回だけ進め、ワーカーごとには更新しない。

主ワーカーは深さ1から通常どおり反復深化する。
補助ワーカーkは、深さdのうち`d == 1`または`(d - 1) % (k + 1) == 0`を満たす反復だけを、深さ上限まで探索する（k=1なら1、3、5、…）。
飛ばした反復の分だけ補助ワーカーは主ワーカーより先の深さへ進み、主ワーカーが同じ深さへ到達したときに共有置換表の記録がカットオフと指し手順序付けに使われる。
2026年8月22日の診断では、補助ワーカーが同じ深さを時間的に先行して探索している間だけ主ワーカーのノードが減り（深さ5で22%減）、補助ワーカーが1つ深い反復にいる間は逆に補助ワーカー側が主ワーカーの記録から利益を得ることが分かった。
後者の利益を捨てないため、各ワーカーは完了した反復ごとに最善手、評価値、深さ、および主変化を保持し、調整役は全ワーカーのjoin後に最も深い反復を完了したワーカーの結果を採用する。
同じ深さなら主ワーカーを優先し、`Threads=1`では主ワーカーしかいないため結果は初版と変わらない。
ルート合法手列の回転は使わない。

補助ワーカーが深さ上限へ到達しても、主ワーカーと他の補助ワーカーを停止しない。
主ワーカーが深さ上限またはsoft limitへ到達した場合と、いずれかのワーカーがhard limitまたはノード上限へ到達した場合は、共通停止状態を更新して全員を巻き戻す。

## 共有置換表

現行の`Vec<Entry>`と`&mut TranspositionTable`は複数ワーカーから共有できないため、置換表の操作を`&self`で行う原子型へ変更する。
1エントリは次の16バイトとし、現行と同じ容量計算とスロット数を維持する。

| 原子値 | 内容 |
|---|---|
| `AtomicU64 critical` | 検証キー32ビット、評価値16ビット、深さ8ビット、バウンド2ビット、および予約6ビット。 |
| `AtomicU64 advisory` | 指し手25ビット、世代8ビット、および予約31ビット。 |

指し手は`from`、`mid`、`to`の各8ビットと成り1ビットで25ビットに収まる。
評価値によるカットオフへ必要な検証キー、評価値、深さ、およびバウンドを1回の原子的読み取りで得るため、競合中も異なる書き込みの値を組み合わせない。

書き込み側は`advisory`をRelaxedで書いた後に`critical`をReleaseで公開し、読み取り側は`critical`をAcquireで読んでから`advisory`をRelaxedで読む。
競合する別の書き込みにより、記録手または世代だけが`critical`と異なる書き込みに由来する可能性は許容する。
記録手は指し手順序付けにしか使わず、復号と現局面の合法手列への所属を検証してから採用するため、取り違えても評価値による誤ったカットオフは起こらない。
世代は置換優先度にしか使わないため、取り違えは書き込みの採否だけに影響し、探索値の正しさを変えない。

書き込み側は読み取った既存値から置換可否を判定し、競合を待たずに2個の原子値を更新する。
置換可否は、空きスロットと過去世代のエントリへは常に書き込み、同世代の異なるキーはより深い結果だけが置換し、同世代の同一キーは新しい深さが既存以上のときだけ置換して浅い結果は保持する（第2版。初版は同一キーへ無条件に書き込んでいたが、静止探索が深さ0の記録を書くようになると通常探索の深い記録を消すため改めた）。
静止探索の記録は深さ0で格納し、通常探索の照会条件`hit.depth >= depth`（深さ1以上）を満たさないため、静止探索の値が通常探索のカットオフに使われることはない。
最後に公開された`critical`が有効な探索値となり、競合で失われた記録手と世代は置換表の助言的情報として扱う。
検証キー、予約ビット、バウンド、および指し手の復号に失敗した場合は置換表の未命中として扱い、不正な`Move`を構築しない。

`clear`と`resize`は探索チームが存在しないときだけ実行する。
`USI_Hash`とCECPの`memory`の変更、新規対局、および規則変更は、既存どおり実行中探索を停止してjoinした後に置換表へ適用する。

## 停止と探索予算

探索チームは、外部停止フラグ、チーム停止フラグ、停止理由、総ノード数、開始時刻、および時間予算を共有する。
外部停止と内部停止を別の原子値にすることで、他ワーカーが立てた停止を`ExternalStop`と誤認しないようにする。

停止理由の優先順位は、`ExternalStop`、`HardLimit`、`NodeLimit`、`SoftLimit`、`DepthCompleted`の順とする。
複数条件が近接した場合は原子値に優先度の高い理由を残し、実行順による表示上の揺れを抑える。

総ノード数は`AtomicU64`で数え、停止フラグとは別のキャッシュライン（64バイト境界）に置く。
初版は全ワーカーが毎ノード共有カウンタへ原子加算し、停止フラグと同じキャッシュラインに置いていたため、加算のたびに他ワーカーが毎ノード読む停止フラグのラインが無効化される偽共有が生じていた。
第2版では各ワーカーが局所カウンタで数え、1024ノードごとに共有カウンタへまとめて加算する。
ノード上限がある場合は1024ノード単位で`compare_exchange`により予約し、残りが1024未満なら残り全部を予約する。予約が0になったワーカーは`NodeLimit`を設定する。
予約した範囲の途中で停止した場合は未使用分を共有カウンタから返却し、`Finished.nodes`が実際に訪問したノード数と一致しNを超えない契約を維持する。

hard limitは全ワーカーが同じ開始時刻から計算した期限と比較する。
スレッド生成時間は利用者が観測する思考時間に含まれるため、開始時刻は補助ワーカー生成前に記録する。
soft limitは主ワーカーのイテレーション境界だけで評価し、補助ワーカーの完了時点では評価しない。

現在の4096局所ノードごとの時計確認は初版で維持する。
ワーカー数に伴う停止遅延は時間制御試験で測定し、時間切れまたは過大なjoin遅延が観測された場合に限り、確認周期を別コミットで変更してGSPRTにかける。

## プロトコル設定

USIの握手では、次のオプションを宣言する。

```text
option name Threads type spin default 1 min 1 max 256
```

`setoption name Threads value N`は1以上256以下の10進整数だけを受理する。
値の欠落、非整数、0、および上限超過には固定した`info string error`を返し、直前の設定を保持する。
探索中に届いた設定変更は既存の入力待機列に載せ、実行中探索を変更せず次の探索から適用する。

CECPでは`feature smp=1`を宣言し、`cores N`を同じ範囲で受理する。
不正値は固定したCECPエラーを返し、少ない値やCPU数へ丸めない。
USIの`Threads`とCECPの`cores`は同じ探索開始引数へ写し、プロトコルごとの並列探索実装を作らない。

`Threads`の既定値は、検証後も1を維持する。
利用可能CPU数を既定値に使うと、同じバイナリの資源消費と再現性が環境に依存するためである。

## 再現性

`Threads=1`では共有置換表への競合がないため、現行の固定深さと固定ノード数の完全再現契約を維持する。
原子型への置換後も、置換規則、容量、スロット番号、および世代幅を変えず、既存benchの総ノード数を一致させる。

`Threads>1`では、固定深さと固定ノード数でも共有置換表の公開順がオペレーティングシステム（OS）のスケジューリングに依存する。
このため、`docs/sprt.md`の完全再現契約を`Threads=1`に限定し、並列時には合法手、予算上限、イベント順序、および停止後の全joinだけを決定的な契約とする。

固定深さbenchは`--threads N`と`--repetitions R`を受け取る。
`Threads=1`かつ反復1回の既定出力は現行の決定性検査に使い、`Threads>1`はウォームアップ1回を除く5回以上の中央値で、到達深さ、総ノード数、経過時間、ノード毎秒、および単一ワーカーに対する速度比を記録する。

## 採否測定

並列化の棋力採否は、`docs/sprt.md`に従って時間制御のコミット対コミットGSPRTで行う。
固定ノード数では計算資源の増加による時間短縮を棋力へ反映できず、固定深さでは両者の探索深さが同じになるため、いずれも主たる採否条件には使わない。

時間制御の水準は測定前に固定する。
初版の測定に使った`time=10000+100,byoyomi=200`は1手あたり約0.4秒、到達深さ4〜5であり、補助ワーカーが先行する余地がほとんどない。
第2版の採否測定は`time=60000+600,byoyomi=1000`（1手あたり数秒、到達深さ6〜7）で行い、結果を見てから水準を変えない。
時間制御GSPRTの前に固定深さbenchのゲートを置く。
ゲートは、深さ5、6、7のそれぞれで、ウォームアップ1回を除く5回の中央値が`Threads=2`で`Threads=1`より15%以上短く、かつ両構成の5回の最大値と最小値の範囲が重ならないこととする。
ゲートに達しない場合は時間制御GSPRTへ進まず、設計へ戻る。

測定用の基準コミットは、並列探索の全コードを含み、`DEFAULT_THREADS=1`としたコミットとする。
候補コミットはこの定数だけを2へ変更し、`match_runner`にはワーカー数を設定する機能比較スイッチを追加しない。
測定後は結果にかかわらず製品の既定値を1へ戻し、利用者がプロトコル設定で明示した場合だけ並列探索を有効にする。

最初に`Threads=2`対`Threads=1`を測定する。
対立仮説H1へ到達した場合だけ`Threads=4`対`Threads=2`を追加測定し、帰無仮説H0へ到達した場合は原因を分析して設計へ戻る。
判定保留の場合は、同じ仮説と時間制御を保ったまま上限ペア数を延長し、都合のよい水準へ変更しない。

時間制御は両者に同じ壁時計条件を与える。
候補側がより多くのCPU時間を使うことは並列規模拡大の測定対象であり、同一CPU時間の比較とは呼ばない。
同時対局数Cは、`C × max(候補Threads, 基準Threads)`が利用可能な物理コア数を超えない値とし、OSとハーネス用に1コア以上を残す。

測定記録には、通常のGSPRT記録に加えて、CPU型、物理コア数、論理コア数、両エンジンのワーカー数、`USI_Hash`、同時対局数、時間制御、および`time_forfeits`を含める。
時間制御対局は完全再現の対象外であるが、着手の非決定性を理由に測定結果を選別しない。

## 実装フェーズ

### フェーズ1　原子型置換表

置換表エントリを16バイトの原子型へ置き換え、`probe`と`store`を`&self`へ変更する。
エントリの詰め込み、公開順序、競合書き込み、世代更新、無効データの未命中化、およびサイズ上限を単体テストで固定する。

このフェーズは`Threads=1`だけで実行し、既存の探索結果とbench総ノード数が完全一致しなければ次へ進まない。
一致しない場合は棋力測定で正当化せず、置換表の意味論を修正する。

### フェーズ2　探索チーム

調整役兼主ワーカー、補助ワーカー、`scope`による全join、ワーカー固有状態、決定的なルート順序分散、および主ワーカーだけの結果採用を実装する。
`Threads=1`の経路も同じ調整役を通し、単一ワーカー専用の旧経路は残さない。

### フェーズ3　共通停止と予算

停止理由、総ノード数、開始時刻、hard limit、およびsoft limitを探索チームの契約へ移す。
外部停止、ノード上限、時間上限、深さ上限、およびワーカーの`panic`について、全joinと1回だけの完了通知を検証する。

### フェーズ4　プロトコルとbench

USIの`Threads`、CECPの`smp=1`と`cores`、およびbenchのワーカー数と反復回数を実装する。
`docs/sprt.md`の再現性契約と並列測定の記録項目を更新する。

### フェーズ5　性能と棋力の採否

固定局面benchで`Threads=1`、2、4の経過時間とノード毎秒を測定し、オーバーサブスクリプションがない条件を確定する。
次に、既定ワーカー数だけが異なる測定コミットを作り、時間制御GSPRTで`Threads=2`対1を判定する。
H1の場合だけ`Threads=4`対2へ進み、各結果を本設計書の実施状況へ記録する。

### フェーズ6　静止探索の置換表と置換規則（第2版）

静止探索で置換表を照会・記録し、同一キーの置換規則を深さで判定する。
静止探索の照会は`enter_node`の直後に行い、ヒットのバウンドがExactか、Lowerで`score >= beta`、Upperで`score <= alpha`ならその値を返す。
出口では最善値を、`beta`以上ならLower、元の`alpha`以下ならUpper、それ以外はExactとして深さ0で記録する。
このフェーズは単一ワーカーの探索結果を変えるため、bench署名を新しい値として記録し、採否は`--each nodes=100000`のコミット対コミットGSPRTで判定する。

### フェーズ7　共有カウンタの配置と一括予約（第2版）

総ノード数を停止フラグと別のキャッシュラインへ分離し、各ワーカーの局所カウンタから1024ノード単位で加算・予約する。
`Threads=1`の固定深さbench署名は不変でなければならない。
採否は固定深さbenchで`Threads=2`と4のワーカーあたりNPSが改善し、`Threads=1`のNPSが低下しないことで判定し、棋力測定は行わない。

### フェーズ8　最深ワーカーの採用と深さスキップ（第2版）

各ワーカーが完了した反復の結果を保持して調整役へ返し、調整役が最も深い反復を完了したワーカーの結果を採用する。
補助ワーカーの反復列を深さスキップへ変更し、ルート回転を撤去する。
採否測定の節に定めた固定深さbenchのゲートを通過した場合だけ、`DEFAULT_THREADS`だけを2へ変えた候補コミットで`time=60000+600,byoyomi=1000`の時間制御GSPRTを行う。

## 検証

各フェーズで次のコマンドを実行する。

```console
cargo test
cargo clippy --all-targets
cargo fmt --all -- --check
git diff --check
```

単体試験と台本試験では、次の契約を検証する。

- 原子型置換表は、同一スロットへの並行`probe`と`store`を反復しても`panic`せず、不正な指し手を返さない。
- `Threads=1`は既存の探索結果、主変化、進捗深さ、ノード数、およびbench署名を維持する。
- `Threads=2`と4は、深さ、ノード、時間、無期限探索からの`stop`、`gameover`、`quit`、および入力終了で全ワーカーをjoinする。
- `Progress.depth`と`Progress.nodes`は単調増加し、`Finished`と`bestmove`は各探索で1回だけ出力される。
- `go nodes N`の`Finished.nodes`はNを超えず、`USI_Hash`の実メモリ量はワーカー数で増えない。
- 探索中の`Threads`、`cores`、`USI_Hash`、および`memory`変更は、実行中探索を変えず次の探索へ反映される。
- 1、2、4ワーカーで短時間制御の反復試験を行い、スレッドリーク、`panic`、反則手、および時間切れがないことを確認する。

並行性の検査は通常テストだけに依存しない。
ThreadSanitizerを利用できるRust nightly環境では置換表の競合試験を実行し、利用できない場合はその事実を検証記録へ明記する。

## 完了条件

- 原子型置換表が16バイト以内であり、`Threads=1`のbench署名と探索結果が実装前に一致している。
- 探索チームの共有状態とワーカー固有状態が設計どおり分離され、停止後に全ワーカーがjoinされている。
- `Threads=2`以上でも、探索予算、イベント順序、合法手、および置換表容量の契約を満たしている。
- USIの`Threads`とCECPの`cores`が、暗黙の丸めや縮退なしに同じワーカー数設定へ接続されている。
- 固定局面benchの並列測定と、`Threads=2`対1の時間制御GSPRTが記録され、GSPRTがH1へ到達している。
- `docs/sprt.md`が、完全再現を`Threads=1`へ限定し、並列測定の資源記録を要求している。
- 全検証コマンドが成功し、時間制御の反復試験で`time_forfeits=0`である。

## 参考資料

- [Stockfish search.cpp](https://github.com/official-stockfish/Stockfish/blob/5062aee519a1ba262d472d8ab139851ced56573e/src/search.cpp)は、主スレッドが補助スレッドを開始し、共有置換表を使う独立探索を終了後に集約する構造の参照先とした。
- [Stockfish thread.cpp](https://github.com/official-stockfish/Stockfish/blob/5062aee519a1ba262d472d8ab139851ced56573e/src/thread.cpp)は、全ワーカーの待機と最終結果選択の参照先とした。
- [Stockfish tt.cpp](https://github.com/official-stockfish/Stockfish/blob/5062aee519a1ba262d472d8ab139851ced56573e/src/tt.cpp)は、原子型フィールドを使う共有置換表と競合読み取りの扱いの参照先とした。
- [やねうら王 yaneuraou-search.cpp](https://github.com/yaneurao/YaneuraOu/blob/33ccf1f907eb7184889fa23051243f81ab0bf973/source/engine/yaneuraou-engine/yaneuraou-search.cpp)は、Lazy SMPと探索終了時のワーカー選択の参照先とした。
- [やねうら王 tt.h](https://github.com/yaneurao/YaneuraOu/blob/33ccf1f907eb7184889fa23051243f81ab0bf973/source/tt.h)は、共有置換表の競合を許容するC++実装との差を確認する参照先とした。

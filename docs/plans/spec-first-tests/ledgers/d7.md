# D7（探索と評価）テスト再構築の追跡台帳

対象: src/search/tests.rs（旧17件）と src/eval/mod.rs の `#[cfg(test)] mod tests`（旧2件）。
旧テスト19件をすべて削除し、挙動マトリクス d7-search-eval.md（32挙動）由来の新テスト33件（search 29件＋eval 4件）へ置き換えた。

## 旧テスト → 新テストの対応

| # | 旧テスト名（ファイル） | 対応する新テスト名 | マトリクスID |
|---|---|---|---|
| 1 | depth_one_finds_capture_of_the_last_royal (search) | depth_one_capture_of_the_last_royal_scores_mate | D7-SRCH-01 |
| 2 | depth_one_prefers_a_free_high_value_capture (search) | free_lion_capture_is_preferred_without_entering_the_mate_band | D7-SRCH-04 |
| 3 | first_of_two_royals_is_material_not_mate (search) | capture_of_the_first_of_two_royals_scores_as_material_gain | D7-SRCH-02 |
| 4 | repeated_child_position_scores_as_draw (search) | repetition_with_history_is_preferred_as_a_draw_over_losing_lines ／ without_history_the_repetition_fixture_scores_a_mate_band_loss | D7-SRCH-03（本則と境界）。旧テストは全子局面を履歴に入れて引き分けを観測するだけだったが、新テストは引き分けが負け筋より選好されることと履歴が探索入力であることまで固定する |
| 5 | same_input_produces_the_same_result (search) | search_with_node_limit_is_deterministic | D7-SRCH-07（初期局面＋中盤フィクスチャ、Progress系列・PV・停止理由まで比較） |
| 6 | node_limit_before_depth_one_returns_first_root_move (search) | node_limit_stops_the_search_with_a_legal_best_move ／ stop_or_tiny_budget_before_depth_one_still_yields_a_legal_best_move | D7-LIM-03／D7-LIM-04。旧テストの「先頭手が返る」assertは、規範節に先頭一致の定めがないため集合帰属（INV-1）へ弱めた（マトリクスD7-LIM-04の方針） |
| 7 | limits_reject_an_unconstrained_search (search) | limits_without_any_constraint_are_rejected_but_explicit_infinite_is_accepted | D7-LIM-02[実装契約]。エラーメッセージ文字列の固定は規範外のため廃止 |
| 8 | clock_budget_uses_integer_coefficients_and_safety_bounds (search) | clock_budget_matches_the_normative_formula | D7-TIME-01。旧テストの端数入力（inc=1000×0.7等は整数演算依存）を、端数の出ない規範導出値（a/b/c）へ差し替え。旧テストの「時計全ゼロでhard=1ms」は優先順位が明文化されていないため（SPEC_UNCLEAR-03）、残り20msの境界でhard≥1msだけをassertする形へ変更 |
| 9 | movetime_is_the_soft_and_hard_budget (search) | movetime_alone_sets_both_soft_and_hard_to_the_given_value ／ movetime_and_clock_combine_per_limit_by_taking_the_smaller | D7-TIME-02／D7-TIME-03。併用則はsoft/hard独立比較の交差例(a)まで固定するよう強化 |
| 10 | movetime_search_stops_near_the_fixed_budget (search) | movetime_search_stops_near_the_fixed_budget | D7-TIME-04。下限を「指定値以上」（旧: 90ms≒指定100msの緩め）へ正し、上限ガードと停止理由2値を維持 |
| 11 | infinite_search_stops_only_after_an_external_request (search) | infinite_limits_stop_only_on_external_request | D7-LIM-05（＋D7-API-03(5)）。旧テストはinfiniteに他制約を同時指定していたが、併用の意味は未規定（SPEC_UNCLEAR-09）のため単独指定へ変更し、「Finishedが届かない」観測を追加 |
| 12 | stop_before_depth_one_returns_the_first_legal_move (search) | stop_or_tiny_budget_before_depth_one_still_yields_a_legal_best_move | D7-LIM-04。旧テストはnodes=0で代用していたが、新テストは停止フラグ（infinite）とmovetime=1msの両経路で検証 |
| 13 | caller_discards_a_delayed_event_with_a_different_search_id (search) | events_carry_the_search_id_given_at_start | D7-API-04（停止→join→新探索のライフサイクル順序を追加） |
| 14 | progress_depths_increase_and_join_returns_a_reusable_table (search) | progress_depths_start_at_one_and_increase_by_one ／ progress_and_finished_elapsed_times_are_monotonic ／ join_returns_the_transposition_table_for_reuse | D7-API-01／D7-API-02／D7-API-05へ分割。API-05は再利用探索の最善手・評価の一致とノード数非増加まで固定するよう強化 |
| 15 | packed_moves_round_trip_for_all_legal_moves_in_multiple_positions (search) | packed_moves_round_trip_across_all_move_shapes | D7-TT-01。居喰い・じっと・2枚取り・端升（dense 0/143）・midなし=0xffのビット層確認・上位ビット0を追加 |
| 16 | mate_scores_round_trip_through_the_table (search) | tt_scores_round_trip_between_root_and_node_relative_forms | D7-TT-02。境界29744/28999、ply=0恒等、±30000×ply256のi16収容を追加 |
| 17 | replacement_rules_cover_same_key_age_and_depth (search) | tt_replacement_follows_same_key_generation_then_depth | D7-TT-03。旧テストに無かった「同世代・同深さは保持」（(5)、`<`/`<=`変異検出）と世代周回（255→0）を追加 |
| 18 | piece_value_table_covers_all_29_piece_kinds (eval) | piece_values_match_the_frozen_table_for_all_29_kinds | D7-EVAL-02。従来の丸写し表をsearch.md「評価関数v0」節の凍結値表（2026-08-14規範化）へ接地。`kind.index()`の並び順assertは実装詳細のため廃止し、網羅は重複なしカバレッジで確認 |
| 19 | initial_position_evaluates_to_zero (eval) | initial_position_evaluates_to_zero_for_either_side_to_move | D7-EVAL-01（後手番としての評価0を追加） |

## 旧テストに対応がなく新設した挙動

- lion_double_capture_of_both_royals_scores_mate — D7-SRCH-05
- opponent_with_no_legal_moves_scores_mate_minus_one_ply — D7-SRCH-06
- depth_limit_accepts_the_1_to_256_range_only — D7-LIM-01
- depth_limited_search_reports_depth_completed_with_a_consistent_pv — D7-API-03(1)＋PV整合
- clock_driven_searches_stop_with_a_time_limit_reason — D7-API-03(3)(4)
- repetition_draw_values_are_not_stored_in_the_table — D7-TT-04
- tt_clear_empties_all_entries_and_search_restarts — D7-TT-05
- idle_resize_applies_and_later_searches_complete — D7-TT-06
- royal_piece_value_exceeds_the_lion_value — D7-EVAL-03
- evaluation_is_symmetric_under_180_degree_rotation — D7-EVAL-04[実装契約]

## 保留・注記

- **D7-TT-07（エントリ16バイト以内）: テスト不能**。`Entry`型はtt.rs内の非公開型で、編集可能なテストモジュール（search/tests.rs）から到達できない。同内容の静的アサーション `const _: () = assert!(size_of::<Entry>() <= 16)` がプロダクション側（src/search/tt.rs）に既存であり、コンパイル時に同じ性質を担保している。テストとしての計上は保留（31/32挙動をテスト化）。
- **D7-TIME-02境界（movetime=0の拒否）: assert置かず**。マトリクスは[実装契約]として拒否を期待するが、現行実装の`SearchLimits::validate`はmovetime=0を受理する。プロダクションコードの修正は本台帳の範囲外のため、この境界はテスト化せず記録に留める（規範文書への追記候補）。
- D7-TT-01のじっと表記: マトリクスは「midは隣接空升」と記すが、本実装の正準表記は獅子指し手正準化により「midなし・to=from」である。規則レベルの意味（第12条第9項）は同じであり、テストは正準表記で検出した。
- 座標規約: マトリクスの筋段表記は`fs(筋, 段) = Square::new(12−筋, 12−段)`で内部座標へ写した（sfen表記の写像と同一）。
- D7-SRCH-01/02/04では捕獲升が敵陣にあり成・不成の2手が同値になるため、SPEC_UNCLEAR-07の方針に従い移動元・到達升だけを固定し、promoteは固定しない。

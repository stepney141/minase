# 領域D8 旧テスト追跡台帳

作成日: 2026-08-15。spec-first再構築フェーズ3の成果物である。
旧テスト25件（src/stats.rs 6件、src/bin/match_runner.rs 16件、src/bin/bench.rs 2件、src/bin/random_play.rs 1件）の行き先を記録する。
新テスト名の後の括弧は接地先マトリクスIDを示す。

## src/stats.rs（6件）

| # | 旧テスト | 行き先 |
|---|---|---|
| 1 | `fishtest_reference_balanced_small_sample` | `fishtest_reference_values_match_within_1e_6`（D8-STAT-01、参照値テーブルの第1行として引き継ぎ） |
| 2 | `fishtest_reference_positive_sample` | 同上（第2行） |
| 3 | `fishtest_reference_regularized_zero_cell` | 同上（第3行。度数0セルの正則化経路検証も引き継ぎ） |
| 4 | `fishtest_reference_large_sample` | 同上（第4行） |
| 5 | `fishtest_reference_fishtest_scale_sample` | 同上（第5行）。加えて左右反転の符号傾向を`mirrored_frequencies_flip_llr_sign_tendency`（D8-STAT-01性質）へ拡張 |
| 6 | `gsprt_monte_carlo_error_rates_are_sane` | `gsprt_monte_carlo_error_rates_match_documented_thresholds`（D8-STAT-05）。反復数3,000と閾値8%/90%は維持。合成分布は旧テストの内部関数`mle_expected`投影をやめ、sprt.mdの写像式から独立に導出した平均0.5/s(5)の分布へ差し替えた |

fishtest参照値5件（1e-6許容）は絶対に失えない外部契約として全件保持した。

## src/bin/match_runner.rs（16件）

| # | 旧テスト | 行き先 |
|---|---|---|
| 7 | `arguments_accept_modes_specs_limits_and_defaults` | 3分割: `documented_defaults_match_sprt_md`（D8-HARN-08/D8-STAT-06、既定値は実装定数参照をやめ文書明文値120/4096/100,000のリテラルで固定）＋`each_limit_is_shared_and_per_engine_overrides_are_optional`（D8-HARN-09）＋`rules_presets_resolve_per_article_33`（D8-HARN-12） |
| 8 | `player_spec_accepts_commit_revision` | `engine_specs_cover_the_documented_three_forms`（D8-HARN-01） |
| 9 | `player_spec_splits_program_and_startup_arguments` | 同上 |
| 10 | `arguments_reject_legacy_player_specs` | 同上（旧`depth=N` spec削除はmatch-harness.md適用範囲に接地） |
| 11 | `invalid_commit_revision_fails_before_match_execution` | `unknown_commit_revision_fails_resolution`（SPEC_UNCLEAR-05 [実装契約]。旧テストのエラーメッセージ文字列照合はやめ、失敗種別のみ検証） |
| 12 | `search_limits_accept_only_supported_forms` | `search_limits_accept_the_documented_grammar`＋`search_limits_reject_malformed_inputs`（D8-HARN-09。depth,nodes併記の受理と拒否細部はSPEC_UNCLEAR-08 [実装契約]と明示） |
| 13 | `search_limits_accept_time_and_optional_byoyomi` | `search_limits_accept_the_documented_grammar`（D8-HARN-09） |
| 14 | `search_limits_reject_time_mixed_with_fixed_limits` | `search_limits_reject_malformed_inputs`（SPEC_UNCLEAR-08 [実装契約]） |
| 15 | `clocks_update_remaining_time_and_detect_time_forfeits` | `time_forfeit_requires_exceeding_remaining_plus_byoyomi`（D8-HARN-10。失権境界「実測>残り+秒読み」の同値側をOkと明示。加算時間の加算タイミング検証はSPEC_UNCLEAR-09につき加算0の時計で回避） |
| 16 | `time_go_text_uses_current_clocks_for_both_colors` | `go_time_arguments_reflect_current_clocks_and_cross_colors`（D8-HARN-10。btime/wtimeの先後交差＋消費後の現在値反映を追加） |
| 17 | `rules_argument_rejects_preset_combined_with_code` | `rules_presets_resolve_per_article_33`（D8-HARN-12。clapエラーメッセージ照合をやめ受理/拒否のみ。LISHOGI大文字・R0拒否・省略時既定R1を追加） |
| 18 | `bestmove_validation_classifies_illegal_responses` | `referee_rejects_bestmove_outside_the_legal_move_list`（D8-HARN-06(1)/D8-HARN-11） |
| 19 | `failure_counts_classify_each_reason` | `failure_reasons_are_counted_separately_and_conserved`（D8-HARN-14。理由別合計の保存則を追加） |
| 20 | `response_channel_classifies_disconnect_and_timeout` | `response_channel_classifies_disconnect_and_timeout`（D8-HARN-06(2)(3)） |
| 21 | `response_channel_ignores_info_before_bestmove` | `response_channel_waits_for_bestmove_ignoring_other_lines`（D8-HARN-11） |
| 22 | `game_results_convert_to_candidate_half_points` | `game_outcomes_map_to_candidate_pair_score_categories`（D8-STAT-04/D8-HARN-06。両色視点とペンタノミアル分類0/2/4への合成を追加） |

新規（旧テストなし）: `decision_labels_match_the_sprt_md_vocabulary`（D8-HARN-13）、`pair_seed_derivation_is_deterministic_nonzero_and_distinct`（D8-HARN-03）、`openings_stay_within_8_to_12_plies_and_derive_deterministically`（D8-HARN-02）。

## src/bin/bench.rs（2件）

| # | 旧テスト | 行き先 |
|---|---|---|
| 23 | `embedded_positions_are_valid_games_with_legal_moves` | `bench_positions_are_the_fifteen_documented_valid_games`（D8-BENCH-01。局面数は旧来の範囲10..=20をやめ、search.md実施状況で確定した15へ固定） |
| 24 | `arguments_reject_zero_depth` | 不採用: benchのCLI深さ検証（0拒否・MAX_PLY超拒否）はD8の規範文書（sprt.md・search.md bench節）に明文がなく、マトリクスの34挙動にも対応がない。挙動自体はコードに残る（テスト対象外） |

新規: `total_node_counts_are_reproducible_across_runs`（D8-BENCH-02。特定ノード数は測定記録として固定せず、再実行間一致だけを契約とする）。

## src/bin/random_play.rs（1件）

| # | 旧テスト | 行き先 |
|---|---|---|
| 25 | `rules_argument_accepts_preset_and_codes_but_rejects_their_combination` | `rules_argument_resolves_presets_case_insensitively_and_rejects_combinations`（D8-HARN-16/D8-HARN-12。clapエラーメッセージ照合をやめ、LISHOGI大文字受理とR0拒否を追加） |

新規: `cli_defaults_flags_and_conflicts_follow_the_design_document`（D8-HARN-16）、`game_seed_follows_the_documented_splitmix64_derivation`（D8-HARN-17。参照値は仕様式から独立計算）、`same_seed_and_game_number_reproduce_the_same_game`（D8-HARN-17性質/18）、`verify_all_verification_completes_on_a_short_game`（D8-HARN-18）。

## 挙動カバレッジ（34挙動）

単体テスト化した挙動（23件）:
D8-STAT-01〜07、D8-HARN-01〜03、06、08〜14、16〜18、D8-BENCH-01〜02。

保留挙動（11件、いずれも本領域の単体テストの範囲外）:

| 挙動 | 保留理由 |
|---|---|
| D8-STAT-08 | LLR取り込み順序の決定性は並列実行の観測が必要。tests/match_runner.rs（統合、本領域の対象外ファイル）の並列度1対Nの出力一致が担保 |
| D8-HARN-04 | 完全再現契約はバイナリ2回実行の全出力比較。統合テストの領分 |
| D8-HARN-05 | 並列度非依存。同上（統合テストが並列度1対4の一致を固定済み） |
| D8-HARN-07 | 手数上限のペア破棄は実プロセス対局を要する。統合テストの領分（run_pair内部のため単体で分離不能） |
| D8-HARN-15 | 検証済み測定の再現は設計上「手動回帰」の位置づけ（マトリクス明記）。常設テスト化しない |
| D8-HARN-19 | random_playの異常時ダンプはprocess::exitを伴い単体テスト不能。集計行書式はSPEC_UNCLEAR-12 |
| D8-RAND-01〜04 | usi_random.rsは本タスクの所有ファイル4件に含まれない（tests/match_runner.rs統合テストとusi_random側の領分） |
| D8-BENCH-03 | bench出力の項目はmainのstdout観測が必要。書式はSPEC_UNCLEAR-15 [実装契約] |

SPEC_UNCLEAR登録簿はマトリクス末尾の15件のとおり。テスト化した[実装契約]は
`unknown_commit_revision_fails_resolution`（SPEC_UNCLEAR-05）、
`search_limits_accept_the_documented_grammar`のdepth,nodes併記行と
`search_limits_reject_malformed_inputs`（SPEC_UNCLEAR-08）で、各テストのコメントに明示した。

探索・評価・統計では145テストを精査した。
以下の「削除」は記載した部分だけを除いてよいという判断、「統合・縮小」は記載した保証を残すことを条件とする判断である。
設計書に計算式が載っている場合でも、テスト内に同じ計算過程をもう1つ実装する必要はない。

以下ではTTを置換表、PSTを駒種と升目による評価表とする。
GSPRTは一般化逐次確率比検定、LLRは対数尤度比を指す。

| 番号 | 判断と対象 | 根拠、削除後の保証、負担 |
|---|---|---|
| S01 | `events_carry_the_search_id_given_at_start` の末尾の `mixed` / `accepted` を削除する。対象は [src/search/tests.rs:2412](/home/stepney141/board-games/minase/src/search/tests.rs:2412)。 | 直前に全イベントのIDを101または202と検査済みであり、以降はテスト内の `chain` と `filter` が指定IDを抽出することしか確認しない。本番の古いイベント破棄が壊れてもここは通る。開始時IDを運ぶ検査は残す。異なるIDのイベントを実ハンドラへ渡して排除する検証は、現行のプロトコルテストにも見当たらない。この既存の不足を、本テストが保証しているとは扱わない。 |
| S02 | `clock_budget_preserves_bounds_over_a_deterministic_grid` の経過時間7値の内側ループを削除する。対象は [src/search/tests.rs:2009](/home/stepney141/board-games/minase/src/search/tests.rs:2009)。 | 継続判定の式をテスト側で再実装して777種類の時計へ適用している。独立な境界例を持つ `next_iteration_requires_both_time_conditions` と `next_iteration_stable_requires_the_prediction_within_soft` を残せば、softの等号誤り、hardの倍率誤り、安定時の条件漏れを検出できる。時計予算自体の安全上限の検査は残す。 |
| S03 | `auxiliary_depth_sequences_follow_the_worker_period_and_include_the_limit` の期待列生成を固定した短い列へ置き換え、同値比較後の `first` / `last` / 昇順assertを削除する。対象は [src/search/tests.rs:2724](/home/stepney141/board-games/minase/src/search/tests.rs:2724)。 | 本番の `(1..=depth_limit).filter(...)` と同じ条件式を期待側へコピーしている。周期2〜5、周期の周回、上限が周期外になる場合、上限1の具体列で十分である。現行25組の列生成と再確認を減らし、開始深さ・周期・最終深さの抜けを独立に検出する。 |
| S04 | `see_values_match_hand_calculated_exchange_sequences` のx-ray例と `see_values_lion_captures_of_non_lions` の後半から、`expected` 配列を逆順に更新するループを除く。対象は [src/search/see.rs:378](/home/stepney141/board-games/minase/src/search/see.rs:378) と [src/search/see.rs:444](/home/stepney141/board-games/minase/src/search/see.rs:444)。 | 両方とも本番SEE（静的交換評価）の利得配列と逆向きのmax計算をコピーしている。局面は保持し、固定した駒価値で各側が交換を中止できる手順を手計算して期待値を置く。これをせず丸ごと消すと、遮蔽が外れた利きや獅子による取り返しの欠落を見逃すため、期待値の置換を条件とする。 |
| S05 | `concurrent_probe_and_store_only_return_written_moves` を削除候補とする。対象は [src/search/tests.rs:3307](/home/stepney141/board-games/minase/src/search/tests.rs:3307)。 | 4スレッド×20,000回で検査するのは、助言語から復号した手が4候補のどれかであることだけである。助言語は1個の `AtomicU64` なので読取りは分断されず、得た値の復号は `packed_moves_round_trip_across_all_move_shapes` で守れる。全probeが未命中でも通り、スコア・深さ・キーの対応は検査していない。4キーの上位32 bitも同一で、異なる検証キーの排除を調べていない。並行処理の実不具合はワーカー停止・ノード総予算・不正な記録手の排除を検査するテストで守る。 |
| S06 | `atomic_tt_entry_is_16_bytes_and_all_fields_round_trip` の `raw_entry` 以降のフィールド再抽出と、冒頭の16 bytesのassertを削除する。対象は [src/search/tests.rs:3213](/home/stepney141/board-games/minase/src/search/tests.rs:3213)。 | 本番と同じMASK・SHIFT定数で格納値を読み直し、その直前の `probe` と同じ結果を再確認している。16 bytesは [src/search/tt.rs:104](/home/stepney141/board-games/minase/src/search/tt.rs:104) のconst assertがコンパイル時に保証している。`probe` で手・スコア・深さ・boundが戻る検査は残し、世代の効果は `tt_replacement_follows_same_key_generation_then_depth` で守る。意味を変えないビット配置変更で壊れる検査と、コンパイル済みの条件の再検査を除ける。 |
| S07 | `malformed_atomic_tt_entries_are_probe_misses` の予約bit設定と不正な升番号144の注入は、防御契約を整理する場合だけ削除候補とする。対象は [src/search/tests.rs:3267](/home/stepney141/board-games/minase/src/search/tests.rs:3267)。 | 有効な `Move` の格納または表の消去からは、そのbit列は生じず、2個の原子語の取り違えでも単語内の升番号は壊れない。ただし [docs/plans/lazy-smp.md:145](/home/stepney141/board-games/minase/docs/plans/lazy-smp.md:145) は復号失敗を未命中とし不正Moveを構築しない契約を明示している。削除で失うのは到達不能な破損値への防御の検査であり、この契約の整理を前提とする。消去直後に実在する無効boundと異なる検証キーの未命中は残す。 |
| S08 | `iir_reduces_depth_when_tt_entry_has_no_move` の `raw_entry` / `write_raw` 操作を削除する。対象は [src/search/tests.rs:1590](/home/stepney141/board-games/minase/src/search/tests.rs:1590)。 | `store(..., None, ...)` が既に「手なし」を保存しているのに、同じ語を `NO_MOVE` で上書きしている。手なしエントリでの減深という実際の保証は、以降の評価・ノード・深さassertに残る。生bitへの依存だけを除ける。 |
| S09 | `repetition_with_history_is_preferred_as_a_draw_over_losing_lines` と `repetition_draw_values_are_not_stored_in_the_table` を1回の探索へ統合する。対象は [src/search/tests.rs:1099](/home/stepney141/board-games/minase/src/search/tests.rs:1099) と [src/search/tests.rs:3101](/home/stepney141/board-games/minase/src/search/tests.rs:3101)。 | 同じ局面・反復手・履歴・深さ2の探索を重複している。後者へ最善手の一致を移し、引き分け0と反復子の未格納を同時に検査する。`without_history_the_repetition_fixture_scores_a_mate_band_loss` は履歴無視を検出する対照として残す。未命中をply 0と1の両方で確認する末尾assertも1つでよい。 |
| S10 | `progress_and_finished_elapsed_times_are_monotonic` と `depth_limited_search_reports_depth_completed_with_a_consistent_pv` を `progress_depths_start_at_one_and_increase_by_one` へ統合する。対象は [src/search/tests.rs:2296](/home/stepney141/board-games/minase/src/search/tests.rs:2296)、`:2316`、`:2254`。 | いずれも同じ `quiet_midgame` から通常の深さ制限探索を起動する。既存の深さ6探索で、時間の単調性・停止理由・PV（主変化）の合法性・最善手との一致を検査すれば、2回の探索とスレッド起動を除ける。深さ1の境界探索と、aspiration再探索を1回だけ通知する保証は残す。 |
| S11 | `events_carry_the_search_id_given_at_start` の独立した探索2回を、S10の探索と即時停止テストへ吸収する。対象は [src/search/tests.rs:2383](/home/stepney141/board-games/minase/src/search/tests.rs:2383)。 | 異なるIDを指定した探索から出た各イベントを検査すればID配線は守れる。古い探索と現在の探索を順番に終える専用台本は、本番の破棄処理を呼んでいない。異なるIDの実イベント排除は現状でも未検証であり、削除によって失う保証ではない。同じ探索をイベント属性ごとに作り直す負担を減らす。 |
| S12 | `multi_worker_teams_finish_once_and_return_the_shared_table` の2・4ワーカー×4停止条件の直積を縮小し、4ワーカーの深さ・ノードのケースを専用テストへ統合する。対象は [src/search/tests.rs:2565](/home/stepney141/board-games/minase/src/search/tests.rs:2565)、`four_worker_node_limit_never_exceeds_the_team_budget`（`:2614`）、`four_worker_fixed_depth_finishes_at_the_limit_with_a_legal_move`（`:2783`）。 | 深さ・ノード・時間・外部停止の4経路は残し、Finishedが1回、チャンネル切断、join後の表返却を同じ実行で調べる。2と4の双方ですべてを反復する必然性はなく、4なら補助ワーカー間の競合も含む。失うのは追加のスケジューリング標本であり、別の仕様分岐ではない。 |
| S13 | `four_worker_progress_is_main_worker_only_and_monotonic` を4ワーカーの深さ制限テストへ統合する。対象は [src/search/tests.rs:2641](/home/stepney141/board-games/minase/src/search/tests.rs:2641) と `:2783`。 | Progressの深さ列・時間・ノード・PVと、Finishedの採用深さを同じ公開非同期探索で検査できる。`run_search_team`、手作りchannel、内部 `WorkerOutcome` への依存を除き、探索を1回減らす。全深さが必ず報告されるとは要求せず、観測列の連続性と最終結果との整合を残す。 |
| S14 | `infinite_limits_stop_only_on_external_request` の300 msにわたる待機を縮小する。対象は [src/search/tests.rs:1903](/home/stepney141/board-games/minase/src/search/tests.rs:1903)。 | 最初のProgressを受けた後に外部停止し、停止理由と合法手を確認する。ただし先に `time_budget(&infinite_limits()).is_none()` の明示検査を追加する。現行にこの直接検査はなく、待機だけを削ると誤った暗黙の100 ms予算による早期終了を見逃す。代替後も実行経路だけに生じる早期終了の時間窓内観測は失うが、無期限指定から予算生成への接続、外部停止、joinを短い検査で残せる。 |
| S15 | `clock_driven_searches_stop_with_a_time_limit_reason` のsoft=hardとなる2回目の実時間探索を削減する。対象は [src/search/tests.rs:2359](/home/stepney141/board-games/minase/src/search/tests.rs:2359)。 | 1回目の時計探索で時計指定から停止までの接続を守り、予算の安全上限は `clock_budget_matches_the_normative_formula`、soft=hardでの実停止は `movetime_search_respects_the_hard_limit_and_returns_a_legal_move` が守る。ただし2回目は残時間0・加算0・秒読み100 msの入力である。削除前にこの指定を `SearchLimits` から `time_budget` へ渡し、soft/hardがともに70 msとなる検査を残す。`clock_budget` の単体検査とmovetimeだけでは、残時間0を誤って無期限扱いする配線の回帰を見逃す。現行2例はいずれもSoftLimitまたはHardLimitを許しており、この代替後は同じ停止機構の実時間標本1つを削減できる。 |
| S16 | `lmr_reduction_preserves_remaining_depth` と `lmr_large_move_indices_use_last_column` の直積を境界代表へ縮小する。対象は [src/search/tests.rs:380](/home/stepney141/board-games/minase/src/search/tests.rs:380) と `:400`。 | 前者は257深さ×256手番号×7historyの460,544回を検査するが、固定上限・浅い深さ・履歴閾値を掛け合わせた同じ不等式を繰り返す。深さ0・1・2・3・4・最大、減深0と上限を与える手番号、historyの閾値直前・同値と両極値を残す。列上限は255・256・最大の対照に集約できる。失うのは中間セルだけに固有の異常の検出であり、表生成式と代表値は `lmr_table_follows_logarithmic_rule` が守る。 |
| S17 | `lmr_history_adjustment_precedes_clamping` の深さ5・手番号255に対する同じ期待値3の9回assertを1〜2例へ縮小する。対象は [src/search/tests.rs:359](/home/stepney141/board-games/minase/src/search/tests.rs:359)。 | 履歴の全9値で上限に張り付く結果を再確認している。履歴閾値の前後は深さ4・手番号8の本体に残し、上限を越える最小・最大の補正だけを検査すれば、補正前に切り詰める誤りと上限漏れを検出できる。 |
| S18 | `packed_moves_round_trip_across_all_move_shapes` の局面生成と全合法手走査を、少数の明示的な `Move` へ縮小する。対象は [src/search/tests.rs:2813](/home/stepney141/board-games/minase/src/search/tests.rs:2813)。 | パッカーが扱うのはfrom・mid・to・promoteだけであり、捕獲・跳躍・駒種は入力に存在しない。端升0/143だけでなく、各fieldのbit4・5・6も検査する16/32/64や0x55/0x2aの相異なる有効升番号、mid有無、from=to、成り、手なしを残す。往復と独立した既知符号値の照合でmaskの欠落やfield混同を検出し、8個の `saw_*` と盤面の維持を除く。設計書の「全合法手を列挙」という検証方法の記述は合わせて変更する。 |
| S19 | `tt_scores_round_trip_between_root_and_node_relative_forms` の9スコア×5plyの同一ply往復を縮小する。対象は [src/search/tests.rs:2912](/home/stepney141/board-games/minase/src/search/tests.rs:2912)。 | 同一plyの往復だけでは、格納時と復号時が両方無変換でも通る。末尾の異なるplyでの正負詰み境界と通常値は残し、最大値・最大plyを含む端点の往復へ集約する。45組の多くは同じ相殺の確認であり、詰み距離の現実的な誤りを独立に検出する部分を中心にする。 |
| S20 | `idle_resize_applies_and_later_searches_complete` のリサイズ前の探索を削除し、リサイズによる既存記録の消去を直接検査する。対象は [src/search/tests.rs:3179](/home/stepney141/board-games/minase/src/search/tests.rs:3179)。 | 現行はresizeが何もしなくても両探索が合法手を返せば通る。既存キーの未命中とリサイズ後1回の探索に集約すれば、取り残した表の再利用という現実的な不具合を守りながら、1回の探索を除ける。失敗resizeで旧記録を保持する保証は `invalid_table_sizes_are_reported_without_panicking` に残す。 |
| S21 | `royal_piece_value_exceeds_the_lion_value` を削除する。対象は [src/eval/handcrafted.rs:241](/home/stepney141/board-games/minase/src/eval/handcrafted.rs:241)。 | `piece_values_match_the_frozen_table_for_all_29_kinds` が王・太子2600と獅子2500を既に固定しており、等値・大小関係も含意する。削除により新たに見逃す不具合はない。規範表そのものの29値の照合は、駒種と配列添字の取り違えを検出するため残す。 |
| S22 | `decode_rejects_checksum_mismatch` を削除する。対象は [src/eval/pst.rs:786](/home/stepney141/board-games/minase/src/eval/pst.rs:786)。 | `checksum_covers_both_endpoints_and_piece_values` が同じ先頭重みの1bit改変を含み、両端点と駒価値の改変まで確認している。現実的な検出力は包含されており、専用テストは不要である。 |
| S23 | `decode_rejects_non_positive_reachable_piece_value` を `stored_piece_values_are_validated_independently_of_weights` へ統合する。対象は [src/eval/pst.rs:873](/home/stepney141/board-games/minase/src/eval/pst.rs:873) と `:1105`。 | 後者は歩兵を含む全到達状態の0を既に拒否する。前者固有のエラーのkind/promoted/valueを歩兵ケースへ移せば、専用フィクスチャと重複decodeを除ける。後者の端点変更でも探索用駒価値を保持する保証は残す。 |
| S24 | `decode_rejects_out_of_range_piece_values` の47状態×4値を縮小する。対象は [src/eval/pst.rs:1068](/home/stepney141/board-games/minase/src/eval/pst.rs:1068)。 | 全状態で-1と29000の境界違反を検査し、`i32::MIN/MAX` は1つの代表状態で確認すればよい。極値を全47状態へ繰り返すたびに約55 KBの検査和再計算とdecodeが走る。状態走査の抜けと上下限の誤りを残し、極値と特定状態の組合せにだけ発生する異常の検出を手放す。 |
| S25 | `decode_rejects_inconsistent_royal_values` の各王駒に対する0・28999のケースを削減する。対象は [src/eval/pst.rs:1053](/home/stepney141/board-games/minase/src/eval/pst.rs:1053)。 | 期待値2600の直下2599・直上2601を王と太子の両方で検査すれば、両側の不一致と片方の検査漏れを守れる。型全域の上下限はS24のテストへ集約する。0や遠い不一致値の反復は同じ拒否条件を再確認している。 |
| S26 | `accumulator_updates_match_full_refresh_across_move_shapes` の埋め込み重みによる1巡を削り、`distinct_pst` による巡回を残す。対象は [src/eval/pst.rs:573](/home/stepney141/board-games/minase/src/eval/pst.rs:573)。 | 異なる端点・特徴値を持つ検査用重みは端点取り違えや差分漏れを識別でき、同じ着手一式を採用重みで再実行する必要は薄い。埋め込み重みの読込み・実評価は `embedded_pst_matches_python_initial_position_evaluation` と探索テストが守る。差分更新と全再計算の照合自体は独立経路なので削除しない。 |
| S27 | `identical_endpoints_match_single_table_evaluation` の8駒数×2手番を、端点と中間の少数例へ縮小する。対象は [src/eval/pst.rs:892](/home/stepney141/board-games/minase/src/eval/pst.rs:892)。 | 両端点が同一の場合の相殺を確認するテストであり、駒数境界と先獅子を除外する保証は `distinct_endpoints_follow_phase_boundaries_and_exclude_lion_feature` が持つ。後手視点は反射テストにある。等端点で元の1表評価へ戻ることを代表例で残し、同じ式の反復を除く。 |
| S28 | `distinct_endpoints_follow_phase_boundaries_and_exclude_lion_feature` の駒数×手番×先獅子の直積を分離する。対象は [src/eval/pst.rs:919](/home/stepney141/board-games/minase/src/eval/pst.rs:919)。 | 駒数0・1・2・3・47・92・93・144の境界は片手番・先獅子なしで確認し、先獅子あり/なしの比較は係数が変わる中間1例、後手は反射テストへ集約する。現行32局面の多くは独立な同じ係数を反復する。失うのはこれらの要因の特定組合せにだけ出る異常の検出である。 |
| S29 | `interpolation_clips_both_signs` の47・92・144枚の反復を縮小する。対象は [src/eval/pst.rs:994](/home/stepney141/board-games/minase/src/eval/pst.rs:994)。 | 正負それぞれの飽和と、最大枚数での桁あふれ防止を残し、中間・序盤の係数境界はS28に集約する。最大絶対重みと最大枚数を残すため、実際に困る評価上限越えや中間演算の桁あふれは守れる。 |
| S30 | `evaluation_matches_rank_reflection_with_colors_swapped` の反射前局面への `assert_evaluation(..., evaluate(...))` から、公開評価の再比較だけを削り、駒数4種類を代表へ縮小する。対象は [src/eval/pst.rs:1014](/home/stepney141/board-games/minase/src/eval/pst.rs:1014)。 | helper内の1個目のassertは自己比較だが、2個目は累算評価との一致なので区別する。中間の非対称局面における白番累算評価との一致と、反射後の公開・累算評価との一致は残す。これによりS28の手番直積削減と同時に行っても、白番で端点や視点を取り違える不具合を検出できる。係数境界はS28へ集約する。 |
| S31 | `every_piece_code_maps_to_the_specified_state` の動的な期待状態番号の計算を固定した対応表へ置き換え、番号一致後の範囲assertを削除する。対象は [src/eval/features.rs:98](/home/stepney141/board-games/minase/src/eval/features.rs:98)。 | 本番と同じ `PieceKind::ALL` と `can_promote()` から番号を導いており、両側が同じ誤った順序を使っても通る。状態番号はPythonとの交換形式なので全状態の対応は残すが、期待値は文書上の番号表に固定する。獅子と成麒麟の同一化、金と成歩の区別は、その固定表へ吸収できる。 |
| S32 | `active_feature_count_matches_pieces_and_optional_lion_square` の独立2局面を特徴列の反射テストへ統合する。対象は [src/eval/features.rs:188](/home/stepney141/board-games/minase/src/eval/features.rs:188) と `:131`。 | 既に走査する初期配置と先獅子つきの非対称局面で、特徴数を確認すればよい。単なる反射一致だけでは双方の特徴が欠落しても通るので、件数assertは移して残す。局面構築と列挙の重複だけを除く。 |
| S33 | `tactical_move_classification_rejects_a_move_without_an_origin_piece` の局面cloneと不変assertを削除する。対象は [src/eval/training_data.rs:808](/home/stepney141/board-games/minase/src/eval/training_data.rs:808)。 | 本番へ渡すのは共有参照 `&Position` であり、この型に内的可変性はない。不正な着手の型付きエラーは残せば、空升からの着手でpanicする不具合を守れる。コピーした局面との比較は型が既に排除する変更を再確認している。 |
| S34 | `persistent_piece_codes_use_disjoint_color_ranges_and_reject_gaps` の不正値ごとの `decode_piece` と `Record::decode` の二重照合を後者へ集約する。対象は [src/eval/training_data.rs:886](/home/stepney141/board-games/minase/src/eval/training_data.rs:886)。 | `Record::decode` が同じ駒decodeを呼ぶため、不正コードの外部入力経路を残せば拒否漏れを検出できる。全不正値の列挙はu8の小さい定義域なので維持してもよいが、同じ値を2つの層で拒否させる必要はない。テスト内で算出した `unpromoted_code` の範囲assertも本番を通らないので削れる。 |
| S35 | `fishtest_reference_values_match_within_1e_6` は、旧仮説の検証契約を整理する場合だけ削除候補とする。対象は [src/stats.rs:182](/home/stepney141/board-games/minase/src/stats.rs:182)。 | 現在の公開関数はH1=10を固定し、同じ5分布を `public_gsprt_llr_matches_h1_10_fishtest_reference_values_within_1e_6` で照合する。ただし [docs/sprt.md:187](/home/stepney141/board-games/minase/docs/sprt.md:187) は旧H1=5と現行H1の各5参照を単体テストで固定することを明示している。削除で失うのは製品経路では使わない旧H1の互換検証である。この契約を廃止し、旧参照値を資料に保存する判断を伴うので、そのまま削除できる重複には数えない。 |
| S36 | `logistic_elo_mapping_matches_documented_formula` を削除候補とする。対象は [src/stats.rs:264](/home/stepney141/board-games/minase/src/stats.rs:264)。 | 私的 `logistic_score` の製品呼出しは0と10だけであり、公開GSPRTの外部参照が換算係数の誤りを検出する。5・±400・123などの追加値と単調性・点対称の再確認は、製品から到達しない入力の保証である。逆変換を使うElo推定は別関数なので、その公開テストは残す。 |
| S37 | `gsprt_bounds_equal_log_error_rate_ratios` を `gsprt_decision_is_three_way_and_exhaustive` へ統合する。対象は [src/stats.rs:236](/home/stepney141/board-games/minase/src/stats.rs:236) と `:247`。 | 閾値定数を読み出してその外側を入力する検査では、閾値が一緒に変わる。誤り率0.05から導いた境界の直内・直外を公開判定へ与えれば、式と判定の2層を1つの境界表で守れる。独立した定数一致テストをなくしても、誤った閾値と符号の不具合を検出できる。 |
| S38 | `symmetric_frequencies_estimate_zero_elo_with_interval_containing_zero` を `elo_confidence_interval_uses_the_95_percent_normal_margin` へ統合する。対象は [src/stats.rs:282](/home/stepney141/board-games/minase/src/stats.rs:282) と `:294`。 | 同じ度数 `[10,25,45,25,10]` を評価している。点推定0を後者へ移し、上下端は `score_to_elo` を使わず独立計算した値と照合する。区間が0・点推定を含むassertはその一致から従うため削れる。z値1.96の回帰検出を残し、内部変換関数を期待側へ使う依存も除ける。 |
| S39 | `mirrored_frequencies_flip_elo_sign` と `scaling_frequencies_preserves_estimate_and_narrows_interval` の同じ元分布の評価を共有する。対象は [src/stats.rs:315](/home/stepney141/board-games/minase/src/stats.rs:315) と `:324`。 | 両方が `[1,2,4,3,1]` を再評価するため、元分布・反転・10倍の3回に集約できる。符号反転と標本数増による区間縮小は別の実不具合を守るので、assertは両方残す。関数名だけをまとめる目的ではなく、同じ標本設定を一箇所にする小さな整理であり、優先度は低い。 |
| S40 | `gsprt_monte_carlo_error_rates_match_documented_thresholds` は常時実行から検定変更時の測定へ移す候補とする。対象は [src/stats.rs:385](/home/stepney141/board-games/minase/src/stats.rs:385)。 | 3,000反復×帰無・対立分布の検証は、数式と停止規則の校正である。公開LLRの外部参照と独立した境界判定を残せば、日常の転記・配線誤りを検出できるが、参照点以外での累積的な統計誤りは通常実行で検出できなくなる。したがって測定を捨てず、専用コマンドと検定式・正則化・停止規則を変更した際の実行条件を [docs/sprt.md](/home/stepney141/board-games/minase/docs/sprt.md) に記録することを条件とする。固定seedなので不安定と断定しない。単独実測は0.47秒、全libは0.65秒であり、速度上の最優先候補ではない。 |
| S41 | `staged_picker_classifies_capture_and_quiet_tt_moves` の先頭以外を再列挙する `while` と、先頭の既知booleanを `move_order_key` でも再確認するassertを削除する。対象は [src/search/tests.rs:858](/home/stepney141/board-games/minase/src/search/tests.rs:858)。 | このテスト固有の保証は捕獲・非捕獲の置換表記録手が先頭に出る際の分類であり、`picked == (tt_move, expected_capture)` に残る。同じ局面の後続手の分類は `staged_picker_classifies_all_generated_captures` が再確認し、記録手との重複排除は `staged_picker_yields_every_legal_move_exactly_once` が守る。 |
| S42 | `staged_picker_yields_every_legal_move_exactly_once` の `illegal` の出現回数0のassertを削除する。対象は [src/search/tests.rs:787](/home/stepney141/board-games/minase/src/search/tests.rs:787)。 | 直前の長さ一致と合法手集合との完全一致が既に不合法手の混入と重複を排除する。集合が正しいのにこのassertだけが失敗することはなく、追加の本番不具合を検出しない。 |

[src/search/tests.rs](/home/stepney141/board-games/minase/src/search/tests.rs) 88件、[src/search/see.rs](/home/stepney141/board-games/minase/src/search/see.rs) 9件、[src/eval/pst.rs](/home/stepney141/board-games/minase/src/eval/pst.rs) 22件、[src/eval/handcrafted.rs](/home/stepney141/board-games/minase/src/eval/handcrafted.rs) 4件、[src/eval/features.rs](/home/stepney141/board-games/minase/src/eval/features.rs) 3件、[src/eval/training_data.rs](/home/stepney141/board-games/minase/src/eval/training_data.rs) 6件、[src/stats.rs](/home/stepney141/board-games/minase/src/stats.rs) 13件の全本文を確認した。
[src/search/tt.rs](/home/stepney141/board-games/minase/src/search/tt.rs) のテスト用アクセサと [src/rng.rs](/home/stepney141/board-games/minase/src/rng.rs) には独立テストはなく、利用先の検査と照合した。
保持する固有の保証には、王駒1枚目と最後の捕獲の区別、獅子による両王駒捕獲、反復履歴の有無、futilityによる偽の詰みの防止、探索中断時の最後の完了結果、置換表の浅い記録による誤カット、異なるplyの詰み距離、停止・panic時の全ワーカー回収、与えたPSTの使用、差分評価と全再計算、入力破損の拒否がある。
これらは内部関数を直接呼ぶテストも含め、消すと具体的な不具合を見逃すので保持する。
